# SFX（同梱音源）

`boom.wav`（カードの表示瞬間）・`riser.wav`（クライマックス直前の高まり）・
`whoosh.wav`（主要カット切り替え）の3ファイルをこのディレクトリに置いてください。
フリー音源（CC0など）や Stable Audio で1回だけ生成したものでも構いません。
3ファイルのうちどれか1つでも欠けている場合、`lib/film-pipeline.ts` は自動的に
SFXなし（音楽のみ）で組み立てます — パイプラインが壊れることはありません。
これらの `.wav` ファイルはリポジトリにコミットしないでください（オーナーが用意）。
