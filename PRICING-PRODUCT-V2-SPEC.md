# 価格・商品設計 v2 仕様書（2プラン + 生成後アドオン + カスタム動画）

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。
> 現行の3ティア（Digital $75 / Feature $129 / Collector $199、物理同梱）を、
> **2プラン（動画のみ）＋ 生成後アドオン（ポスター/キャンバス）** に作り替える。
> オーナー確定事項を反映済み。作成: 2026-07-24

---

## 1. 新しい商品構造（確定）

### 2プラン（動画 ＋ デジタルポスター無料同梱。物理だけアドオン）

| | **Preset Worlds** | **Director's Cut（カスタム）** |
|---|---|---|
| 価格 | **$99** | **$249** |
| 動画 | 60秒トレーラー。既存3ワールド（Deep Space / Storybook / Noir）から選ぶ | 60秒トレーラー。ユーザーの自由記述ブリーフから **Claudeが台本＋世界観を生成** |
| デジタルポスター | **✅ 無料同梱** | **✅ 無料同梱** |
| 物理ポスター/キャンバス | アドオン | アドオン |
| 制作フロー | 従来 `FILM_SCRIPTS` を使用 | Claudeフロー（§2〜3） |

- **確定1（更新）**: デジタルポスターは **両プランとも無料同梱**。有料アドオンは**物理のみ**。
  - 理由: 既に生成済みで限界コスト≈0。無料で見せることで**物理アップセルの"撒き餌"**になり（デジタル現物→物理が欲しくなる）、ベースの価値も厚くなる。デジタル単体課金は元々弱い商材。
- **確定2**: プラン2ではClaudeが**視覚ワールド/美術（衣装ロック・スコア・世界観）も定義**する。

### 生成後アドオン（＝感情ピークでのアップセル・物理のみ）

デジタルポスターは同梱済み。動画が完成し顧客が「うちの子だ！」となる瞬間（納品＝Gate 2 後）に、**2回目の決済**で**物理**を追加購入できる:

| アドオン | 内容 | 価格（確定） | 粗利目安 |
|---|---|---|---|
| Printed Poster | 16×24 ポスター印刷・配送（Printify、送料込み） | **$59** | ≈$31（52%） |
| Gallery Canvas | 16×24 ギャラリーキャンバス・配送（送料込み） | **$99** | ≈$50（50%） |

- 原価（Phase 5検証済み・豪州宛でも黒字）: ポスター実費≈$28、キャンバス実費≈$49。
- **ポスターアートは動画生成と一緒に先に作る**（`poster-pipeline` は既存）。デジタル版は納品時に無料で渡し、**同じアートの物理印刷**をアップセル（「あなたのポスターを額装品質のキャンバスで」）。
- **導線（確定）**: **納品ページ（動画を見た直後＝感情ピーク）を本命**＋その後も注文ステータス画面から追加可。**Gate 1（絵コンテ承認）には出さない**（感動前・ポスター未確定で早すぎる）。

---

## 2. カスタム動画フロー（プラン2）— Claude台本パイプライン

ユーザーの自由記述 → Claude整形 → Claude台本構成 → 従来パイプライン。
（台本生成は **Claude API**、本人そっくりのスコアリングは従来どおり **Gemini VLM**。§3.1）

```
1. ユーザーが「映像シーンの要望」を自由記述（ガイド付き入力・§4）
2. [Claude] モデレーション + IP/フランチャイズ・ガード（§3ガード2）→ 却下 or サニタイズ
3. [Claude] ブリーフを整理・正規化
4. [Claude] 6カットの物語 + 視覚ワールド一式を「構造化JSON」で生成（Tool use・§3）
5. 従来パイプライン: ヒーローシート → 絵コンテ生成 → Gate 1 承認 → 動画 → 本人判定ゲート（Gemini VLM）→ Gate 2 納品
```

**この設計が成立する最大の理由**: **Gate 1（顧客が絵コンテを承認）** が既にある。台本の解釈がズレても、**重い動画生成の前に**顧客が気づいて弾ける。カスタムの品質リスクを構造的に吸収する安全弁。

---

## 3. Claudeが生成すべきもの（既存パイプライン互換の“ワールド一式”）

### 3.1 LLMプロバイダの役割分担（確定）

- **台本生成＋モデレーション/IPガード = Claude API**。理由: 創作台本の質、複雑な制約遵守（§3の4ガード）、IP/フランチャイズのニュアンス言い換え、Tool useでの構造化出力に強い。$249に対しLLMコストは誤差なので**質・相性で選定**。モデル: Sonnet を既定（質・コスト均衡）、最高品質狙いは Opus。**モデル確定は実装時**。
- **本人そっくり/リアリティのスコアリング = Gemini VLM のまま**。既にパイプライン組込済みで、"画像を見て採点"する別ジョブ＝適材適所。
- 留意: Anthropic APIキー＋SDKが1つ増える（軽微）。

### 3.2 生成すべきワールド一式

現行パイプラインは各ワールドにつき以下を持つ（`lib/film-script.ts`）。カスタムでは Claude がこれと**同じ形**を動的に出す:

- `costume`（**衣装を1つにロック** = `WORLD_COSTUMES` 相当。全カットで同一の服。シーン記述には衣装を書かない）
- `score`（原曲生成用の音楽プロンプト = `WORLD_SCORES` 相当）
- 6カットの `{ scene, framing }`（`FILM_SCRIPTS` + `SHOT_FRAMINGS` 相当。動作＋背景のみ、衣装は書かない）
- `loglines`（カット間のトレーラーテキスト）＋ `tagline`（クロージング）

### 3.3 カスタムを安全にする4つの必須ガード（Claudeプロンプト/層に焼き込む）

1. **本人そっくり維持** — 台本生成時に「顔が大きく・鮮明・良い光（medium/close中心）」を強制。引きの絵・水中・激しい衣装・逆光など**顔が崩れる構図を避ける**。Cut 5で学んだ教訓と同じ。放置すると本人判定ゲートが落ち→無償再制作がコスト爆弾に。
2. **モデレーション + IP/フランチャイズ・ガード** — 自由記述には不適切要望（暴力/性的/実在人物）と**フランチャイズ模倣（例「ジェダイに」「マーベルヒーローに」）**が必ず来る。ブランド原則（模倣しない）＋著作権的に、**拒否またはオリジナル世界観へ言い換え**する層を必須にする（Claudeのニュアンス処理が効く）。プロンプトインジェクション対策も含む。
3. **構造化出力（JSON schema固定）** — Claudeの出力を上記フィールドのスキーマに**強制**（Tool use / structured output）。ステップ5が確実に消費できるように。
4. **期待値マネジメント** — 「6ショット・60秒・スタイライズされたトレーラー・愛犬が主役」という枠を明示。実写4K VFX級を期待させない。

---

## 3.5 「思ってたのと違う」を防ぐ設計（Director's Cut の肝・確定）

**問題**: 入口で$249先払い → 裏でAIが台本設計 → 完成物で「思ってたのと違う」＝**先払いAIガチャ**。高額カスタムの最大リスク。

**対策（層で潰す）**:

### (A) 2段階の承認ゲート＋無料直しの配分（確定）
「文章 → 画像 → 動画」の順で、**安い段階で方向性を固める**。無料の直しは**コストが出る所（画像）だけ上限**をかける。

**段階① トリートメント承認（文章・ほぼ0円）**
1. Claudeがブリーフから **6カットのあらすじ・世界観・衣装・ロゴライン・タグラインを"文章"で提示**（画像はまだ0枚）。
2. 顧客が読んで承認 or 「カット3を深海に」等の指示 → Claudeがリライト。
3. **文章の直しは実質無制限（潤沢）**。顧客が「これ！」となるまで何度でも。※内部的な連投・不正対策の上限のみ（顧客体験上は"納得いくまで"）。ここで大半の"思ってたのと違う"を解消。

**段階② 絵コンテ承認（画像・fal生成コストあり）= 従来 `Gate 1`**
4. 文章トリートメント承認後に絵コンテ生成。既存どおり **1カット×3テイク＝18枚**から顧客が各カット1枚を選ぶ。
5. どのテイクもピンと来ないカットの作り直し＝**リロールは合計3回まで無料**。
   - **「1回」の定義（確定）**: 一度の直し指示で**複数カットをまとめてリロールしても1回**とカウント（バッチ＝1）。
   - 実装は既存の再生成機構（`rerender-shot` / `kickShotRerender`）を顧客導線から呼ぶ。
6. 絵コンテ承認 → 動画レンダリング（ここから高コスト）→ 本人判定ゲート（Gemini VLM）→ `Gate 2` 納品。

- 効果: 一番安い"文章"段階でたっぷりすり合わせ、"完成物で初めて違和感"を構造的に消す。

### (B) 入口をガイド付き＋往復に
空欄一発勝負をやめる（§4）。設定/雰囲気/見せ場/エンディングを軽く分けて入力＋**Claudeが1〜2問だけ聞き返す**（例「宇宙は近未来 or レトロSF?」）→ 解釈のズレを入口で潰す。

### (C) 制作前・返金保証（＋少額の非返金フィー）※確定
高額compute（動画レンダリング）は承認まで発生しない設計なので、下記を謳える:

> **「まず何度でも直します（文章は潤沢、絵コンテのリロールは3回まで無料）。それでも撮影前に承認できなければ $200 を返金。Concept & Storyboard fee $49 は非返金。」**
> 英語コピー例: *"We'll revise it with you — unlimited concept tweaks, plus 3 free storyboard re-rolls. If we still can't land it before filming, your $200 production fee is refunded. The $49 concept fee covers the custom storyboard we build for you."*

- **返金の発動条件（確定・ハイブリッド）**: まず (A) の無料直しで詰める。**段階②の無料リロール3回を使い切ってもなお絵コンテを承認できない**場合に限り、**動画レンダリング前**に $200 返金。3回未使用のうちは"まず直す"が先（＝いきなり返金は不可）。承認・撮影後は受注生産につき返金不可（Preset同様）。
- **金額（確定・要弁護士確認）**: $249 のうち **$49 = Concept & Storyboard fee（非返金）／$200 = 返金**。目的はコスト回収でなく**冷やかし抑止＋実施した創作workの対価**。$39/$210 でも可、既定は $49/$200。
- **正当化**: キャンセルしても顧客は"Claude生成の企画書＋絵コンテ"という実際の創作物を受領済み＝フィーの対価。
- **持ち逃げ防止（必須）**: トリートメント/絵コンテのプレビューは **ウォーターマーク＋低解像度**で承認させ、**クリーンな素材は完成時のみ**納品。→ 返金されても使える成果物を持ち逃げできない。3回リロール＋$49損＋ウォーターマークで、返金目当ての冷やかしは割に合わない。
- **法務（要 Phase 7 弁護士レビュー）**: 非返金フィー＋部分返金は、**購入前に明示**すれば一般に許容される運用（US=開示前提でOK／EU・UK=カスタム品はクーリングオフ対象外になりやすい／日本=特商法で返金条件の明記義務）。ただしクロスボーダーの最終確認は弁護士レビューで。**開示先**: 価格ページ・チェックアウト同意・利用規約・返金ポリシー・特商法ページの全部に記載。
- 以前の「Option A・原則返金なし」は **Preset 前提**。Custom はこの (C) を適用（"気が変わった返金"ではなく"制作前に方向性が合わなければ"なので方針と矛盾しない）。

---

## 4. カスタム入力UX（プラン2）

- **空欄の自由記述は避ける**（悪いブリーフを生む）。ガイド付きにする:
  - 例: 「舞台/設定」「雰囲気（ワクワク・感動・かっこいい等）」「見せ場を1つ」を軽く分けて記入 or 例文プレースホルダ。
  - Claudeのステップ3（正規化）＋1〜2問の聞き返しが受け皿になるので、完璧な入力は不要。
- 入力タイミング: **決済後の intake ステップ**（現状の「写真アップロード＋ワールド選択」と同じ場所）に置く。ブリーフは決済前に取らない。

---

## 5. 実装で触るところ（未実装・後日）

| 領域 | 変更 |
|---|---|
| `components/PricingTeaser.tsx` | 3枚→**2プラン**（$99 / $249）、動画のみ表記、アドオン訴求。比較アンカー文言（現「ポスター付き」）も見直し（ポスターは同梱でなくなるため） |
| Stripe | 新Price（$99 / $249）＋**アドオンPrice**（Printed Poster $59 / Gallery Canvas $99）。**旧3価格（$75/$129/$199）は完全退役**（テストモードで実売ゼロ＝依存顧客なし） |
| `lib/stripe.ts` | `Tier` 型を `preset` / `custom` に再定義。アドオン用のPrice解決を追加 |
| `app/api/checkout/route.ts` | ベース決済（preset/custom）。shipping収集はアドオン購入時へ移す（動画のみなら初回は住所不要） |
| **トリートメント承認ゲート（新規・Custom）** | `Gate 1`の手前に"文章トリートメント承認"を追加（§3.5 A）。**文章の直しは実質無制限**（内部上限のみ）。ステートに `treatmentApproved` 等の段階を追加 |
| **顧客リロール（新規・Custom）** | 絵コンテ段階（`Gate 1`）で顧客が**合計3回まで無料リロール**（複数カットまとめて1回）。既存 `rerender-shot` / `kickShotRerender` を顧客導線から呼ぶ。`storyboardRerollCount` 等でカウント管理。3回消化後にのみ返金ボタンを解放 |
| **制作前・部分返金（新規・Custom）** | **リロール3回を使い切ってもなお未承認**の場合のみ、動画レンダリング前まで $200 返金（$49非返金）。当面はStripeダッシュボードから手動部分返金で運用可。プレビューは**ウォーターマーク＋低解像度**、クリーン素材は完成時のみ（§3.5 C） |
| **アドオン決済フロー（新規）** | 納品ページ（Gate 2後）＋注文ステータス画面に「物理ポスター/キャンバスを追加」CTA → **2回目のStripe Checkout** → Printify発注。注文ステートマシンに**アドオン経路**を追加（2ゲートのコアは壊さない）。デジタルポスターは同梱なので納品時に無料付与 |
| `prisma/schema.prisma`（Order） | `tier`→`plan`（preset/custom）。追加: `customBrief`(text), `generatedScript`(Json = Claudeのワールド一式), `treatmentApproved`(bool/段階), `storyboardRerollCount`(int), アドオン購入状態（例 `addons` Json or 個別フラグ + `addonStripeSessionId`）。物理発送情報はアドオン購入時に埋める |
| 生成パイプライン | preset=`FILM_SCRIPTS`、custom=**Claude生成スクリプト**に分岐。**Claude API連携**（Anthropic APIキー、呼び出し、Tool useで構造化出力、モデレーション/IPガード）を新規追加。本人判定は従来の Gemini VLM を継続 |
| `poster-pipeline` | 生成は従来どおり動画と一緒に**先に作る**。**デジタル版は納品時に無料付与**、**物理印刷はアドオン購入時のみ**発注 |
| 法務/コピー | 受注生産・無償再制作の方針は不変。「ポスター同梱」前提の文言を全て見直し |

---

## 6. 決定事項・残タスク

### 確定済み（このセッション）
- **商品**: 2プラン＝Preset **$99** / Director's Cut **$249**。両プラン**動画＋デジタルポスター無料同梱**。
- **アドオン（物理のみ）**: Printed Poster **$59** / Gallery Canvas **$99**。**導線＝納品ページ（感情ピーク）＋注文ステータス画面**、Gate 1には出さない。
- **旧3ティア**: 完全退役（テストモードで実売ゼロ）。
- **ビルド順**: ①**フェーズA**＝2プランUI＋Preset＋アドオン導線（低リスク・先に売上化）→ ②**フェーズB**＝Director's Cut（Claude台本・トリートメント承認・リロール計数・部分返金）。
- **SLA**: 動画レンダリング＝**絵コンテ承認後48時間**（Preset同様）。トリートメント/絵コンテの往復は**顧客ペース**（初稿24〜48h→一緒に詰める、固定時間は約束しない）。**Director's Cutは1日1〜2枠の限定**（希少性＝プレミアム）。
- **Director's Cut の台本生成** = Claude API（スコアリングは Gemini VLM 継続）。
- **「思ってたのと違う」対策** = ハイブリッド: (A) 2段階承認 → (B) ガイド入力 → (C) 制作前部分返金。
- **無料の直し**: 文章トリートメント=実質無制限／絵コンテのリロール=合計3回まで無料（まとめて複数カット直しても1回）。
- **返金**: リロール3回を使い切ってから、撮影前に **$200返金／$49非返金（Concept & Storyboard fee）**。プレビューはウォーターマーク。
- **Googleログイン**: MVPでは入れない。将来アドオン再購入/Custom往復のニーズが出たら任意アカウントとして検討（メール・マジックリンクの延長が最安、壁にはしない）。

### 実装前に詰める小タスク（Phase/運用で）
1. **Concept & Storyboard fee の最終額**（既定 $49、$39〜可）＋ **弁護士レビューでの返金クラウズ確定**（Phase 7）。
2. **文章トリートメントの内部上限値**（顧客体験は"無制限"、不正対策の内部リミットのみ）。
3. **キャンバス購入者へのボーナス**（任意）: 既にデジタルポスターは全員無料なので、キャンバスには例えば"サイン風エディション番号"等の付加価値を検討可（任意・後日）。
4. **物理サイズ**: ポスター/キャンバスとも 16×24（POD variant 確定済み）で統一。

---

## 7. 評価まとめ（なぜこの設計を推すか）

- **アドオン＝感情ピークのアップセル**: 入口$99で軽く、完成動画の感動直後にポスター/キャンバスを売る → 転換率↑・客単価↑。原価的にも黒字余裕あり。
- **カスタム＝既存パイプライン再利用 + 承認ゲート安全弁**: Claudeが台本を作り、顧客が「文章トリートメント→絵コンテ」の2段階承認で品質を担保。人手依存せずスケールしつつ"自分だけの1本"の特別感を$249で出せる。
- **最大リスクはカスタムの本人再現と不適切要望** → §3.3の4ガードで抑える。
- **"先払いAIガチャ"の恐怖** → §3.5ハイブリッド（潤沢な文章直し＋3回リロール＋制作前$200返金）で解消しつつ、$49非返金＋ウォーターマーク＋"3回消化後のみ返金"で冷やかしも抑制。
