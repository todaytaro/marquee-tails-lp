# MOTION V2 — Seedance 2.0 と「動く映画」への転換

Status: **決定した方向性 / 未実装。** 製品コードは1行も変えていない。
記録日: 2026-08-13
根拠: `scripts/motion-test.ts`（この日回した実測。再実行すれば同じ比較が出る）

---

## 1. 何を変えるか

v1（現行・稼働中）は **「動かさないことで同一性を守る」** 設計だった。v2 はその前提を捨てる。

| | v1（現行） | v2 |
|---|---|---|
| 動画モデル | `fal-ai/kling-video/v3/pro/image-to-video` | **`bytedance/seedance-2.0/image-to-video`** |
| 始点＋終点の固定 | `end_image_url` に承認済み静止画 | **使わない** |
| 動きの指示 | 瞬き・呼吸・耳のぴくつきのみ。ヨー（首の横回転）禁止 | **移動・跳躍・ヨーを含む大きな動き** |
| `negative_prompt` | あり（`CLIP_NEGATIVE`） | **エンドポイントに存在しない** |
| `cfg_scale` | 0.55 | **エンドポイントに存在しない** |

v1 は動きを抑えるブレーキを4つ重ねていた（cfg / negative / プロンプト本文 / end frame）。
**v2 はそのうち3つを失う。** 残るのはプロンプト本文だけ。これは事故ではなく、
オーナーが実際に3本を再生して比較した上での選択。

---

## 2. なぜ Seedance か

同じ静止画（LALA cut 1・艦長席・顧客承認済み）に、同じプロンプトで
3モデルを走らせた。8秒 / 1080p / 24fps。end frame は3モデルとも不使用。

指示した動作は **意図的に v1 の禁止事項を含む**:
椅子から飛び降り → 着地 → カメラへ走る → **左を見る（ヨー）** → 正面に戻って吠える。

| モデル | 生成時間 | 実費 | 結果 |
|---|---|---|---|
| Kling v3 pro | 167秒 | $0.67 | コマ上は同一性が最も残る。ただし negative + cfg 0.55 付き＝**条件が有利** |
| **Seedance 2.0** | 437秒 | 約$2.4 | **採用。** 白カフスなど制服を保持。毛色がやや赤茶に寄る |
| HappyHorse 1.0 | 190秒 | $2.24 | 動きは最大だが**衣装が崩壊**（金ボタン・白ラインが消え、ただの紺セーターに）。指定不能な音声が付く |

**判断はオーナーが動画を再生して行った。** アシスタント側はコマ（1.5秒間隔6枚）しか
見ておらず、そのコマ比較では Kling が優勢に見えていた。動きの滑らかさ・途中の
モーフィングはコマでは判定できない領域で、そこを含めた再生評価で Seedance が勝った。
**この差は記録しておく価値がある — 静止画の比較では動画モデルは選べない。**

なお Kling の優位は制御手段（negative + cfg）の数によるもので、地力の比較ではない。
Seedance にその文言をプロンプト本文へ移植した版は**まだ試していない**（§5）。

---

## 3. v2 が意識的に手放すもの

### 3.1 end frame（始点＋終点の補間）

v1 では承認済み静止画を始点と終点の両方に置き、動画モデルの仕事を
「2枚の承認済みの画の間を埋めるだけ」に限定していた（FILM-QUALITY-V3-SPEC.md §5）。
**v2 はこれを外す。** モデルは自由に動きを発明できる代わりに、終端が
どこへ着地するかの保証を失う。

Seedance 自体は `end_image_url` を持っているので、**後から戻せる。**
外すのは「動きを出す」という今回の目的のためであって、機能が無いからではない。

### 3.2 ヨー禁止のルール

`lib/film-script.ts` の `SHOT_MOTIONS` は、**最初の本番映画の失敗から生まれたルール**だった。
6カット中3カットが「左右を見る」「顔をそむける」を指示し、横顔が別の犬になった。
原因は文言ではなく参照カバレッジ — 正面顔しか学習していないので横顔は必ず捏造される。

**v2 はこれを解禁する。** 今回のテストで Seedance は横顔を破綻させずに出したが、
n=1 の結果である。全6カット・複数犬種で再現するかは未検証。

### 3.3 negative_prompt / cfg_scale

エンドポイントに存在しないため、選択の余地なく失う。
`CLIP_NEGATIVE` が抑えていた要素は**プロンプト本文へ書き直す**必要がある:

```
morphing / different dog / changing costume / wrong tongue color /
wrong tail length / wrong ear shape / deformed face / extra limbs / warped anatomy
```

※ `ears changing` と `tail changing` は今回のテストで**意図的に外した**。
「別の犬の耳・尻尾に変わるな」のつもりが、文字通り読むと尻尾を振る・耳が動くも
該当し、今回いちばん出したい動きを潰すため。形状を縛る `wrong tail length` /
`wrong ear shape` は残す。この2語は v2 でも復活させない。

---

## 4. コスト影響

1注文 = 本編6カット×8秒 + インサート3本×3秒 = 57秒。

| | Kling（v1） | Seedance（v2） |
|---|---|---|
| 単価 | $0.084/秒 | 約$0.30/秒 |
| 動画代 | $4.8 | **$17.2** |
| 1注文の総原価 | $20〜25 | **$33〜37** |

$249 の Director's Cut では誤差。$99 のままの Preset なら粗利が3割以上削られるが、
**その回避策として「モデルで使い分ける」ではなく「Preset を $159 に値上げする」を選んだ**（§4.1）。
同一性ゲートの撮り直し（`MAX_CLIP_REROLLS = 1`）が走ると該当カットは倍。

生成時間は `Promise.all` で並列投入しているため、1本437秒でも全体では7〜8分。
v1 の3分からは伸びるが、映画全体16分に対して許容範囲。

### 4.1 Preset $99 → $159（決定・未実施）

v2 は**全プランに適用する。** モデルをプランで使い分けると「$99 は動きが少ない」という
差になり、商品として説明できない。代わりに Preset の価格を上げて原価増を吸収する。

| | 現行 | v2 |
|---|---|---|
| Preset Worlds | $99 | **$159** |
| Director's Cut | $249 | $249（据え置き） |
| Preset の原価 | $20〜25 | $33〜37 |
| Preset の粗利 | $74〜79 | **$122〜126** |

DC との価格差が $150 → $90 に縮まる。上位プランへの誘導は効きやすくなる一方、
DC の割高感が薄れて Preset に流れる可能性もある。**どちらに転ぶかは未観測。**

**注意 — `lib/safety-net.ts` の `NONREFUNDABLE_FEE_USD = 99` は Preset の価格ではない。**
これは Director's Cut ($249 = 非返金の企画・絵コンテ費 $99 + 返金可能な撮影費 $150) の
内訳。**ただしこの 99 は §4.3 の決定により 59 に変わる。** 混同して $159 に引きずられないこと。

### 4.2 $99 が書かれている場所（値上げ時に全部直す）

Stripe の Price を差し替えるだけでは足りない。表示価格が実際の請求額と食い違うと、
**特定商取引法に基づく表記が事実と違う**状態になる。

| 種別 | 場所 |
|---|---|
| 課金 | Stripe の Price（`STRIPE_PRICE_PRESET`）。金額は変更できないので**新規作成 → 環境変数を差し替え** |
| 法定表記 | `app/(legal)/tokushoho/page.tsx`、`app/(legal)/terms/page.tsx`、`app/(legal)/refund/page.tsx` |
| LP・メタ | `app/layout.tsx`（"from $99"）、価格表示のあるLP各所 |
| 管理画面 | `app/admin/GiftOrderForm.tsx`（"$99相当"） |
| 証跡 | `lib/evidence-text.ts`（チャージバック用の商品説明文） |
| コメント | `lib/stripe.ts`、`app/api/download/route.ts` ほか（動作に影響しないが古い数字が残る） |

**触らないもの:** アドオンのギャラリーキャンバス $99（別商品）。
`NONREFUNDABLE_FEE_USD` は触るが $159 ではなく **59** にする（§4.3）。

### 4.3 返金は一律 $59 を差し引く（決定・未実施）

**どのプランでも、返金する場合は $59 を差し引いて返す。**

| | 現行 | v2 |
|---|---|---|
| Preset $159 | 返金の仕組みなし | 返金額 **$100**（$59 を差し引き） |
| Director's Cut $249 | $150 返金 / $99 非返金 | 返金額 **$190**（$59 を差し引き） |

DC の顧客にとっては**返金額が $150 → $190 に増える**（差し引きが $99 → $59 になるため）。
値上げと同時に返金条件が良くなる形で、値上げの説明としても筋が通る。

$59 の根拠は原価。v2 の1注文原価は $33〜37 で、返金対象になるのは撮影開始前
（＝LoRA学習・絵コンテまで完了、動画は未生成）。その時点までに使い切っている
実費がおおよそ $22（LoRA $9.5 + 絵コンテ $12.4、2026-08 実測）なので、$59 は
実費 + 決済手数料 + 手間をカバーして少し残る水準。

**実装への影響:**
- `NONREFUNDABLE_FEE_USD = 99` → **59**
- `REFUND_AMOUNT_USD = 150` は**単一の定数では表せなくなる**（プランごとに $100 / $190）。
  プラン価格から $59 を引く関数にするか、プラン別の定数2つに割るか。設計は未決
- `canRequestRefund` は現在 `tier === "custom"` 限定。**Preset にも返金導線を作るかは未決**（§5）。
  「一律 $59」は返金する場合の差し引き額を決めたものであって、
  Preset に返金ボタンを生やすと決めたわけではない
- 法定表記3ページ（tokushoho / terms / refund）の金額をすべて更新

### 4.4 やり直しは絵コンテまで。動画の作り直しはしない（決定）

**顧客がやり直せるのは絵コンテ（Gate 1）まで。完成した動画の作り直しには応じない。**

これは現行の実装とほぼ一致しており、**新しく作るものではなく、明文化して守る方針**。
現状:

- `canReroll` … `AWAITING_CUSTOMER_APPROVAL`（Gate 1）でのみ通る。無償3回まで（`STORYBOARD_REROLL_CAP`）
- `canRequestRefund` … 「動画生成が始まったら返金窓口は閉じる」と既に実装済み
- 動画の再生成は `kickShotRerender`（管理者操作）のみ。**顧客導線は存在しない**

v2 で明確にすること:

1. **顧客向けの動画再生成は今後も作らない。** 管理者判断での撮り直しは残す（品質責任はこちら側にある）
2. LP・規約に「動画の作り直しには応じない／直せるのは絵コンテの段階まで」を明記する。
   いまは「作り直さない」とはどこにも書いていない — 書いていないものは期待される
3. だからこそ**絵コンテ承認の画面が最後の砦**になる。ここで納得していない顧客を
   通してはいけない、という運用上の含意がある

**なぜ動画は作り直さないか:** 動画1本が原価の大半（$17.2 / 総額 $33〜37）を占める。
作り直しは実質もう1注文分の原価で、$159 では2回で赤字になる。
一方、絵コンテのやり直しは1回あたり数ドルで、**顧客の不満のほとんどは
「イメージと違う」＝絵コンテの段階で解決できる**。安いうちに直す設計。

---

## 5. 未決 / 未検証

1. **同一性文言をプロンプトに移植した版が未検証。** これを入れれば今回より安定するはず（推定であり実測ではない）
2. **`shotIdentityScores` の閾値。** 現行 `CLIP_IDENTITY_THRESHOLD = 50`。動きを増やせばスコアは下がる方向に働くはずで、50 のままで良いかは要再検討
3. **`SHOT_MOTIONS` の全面書き直し。** 6カット分の動きを v2 用に作り直す必要がある。今回試したのは1カット分の1本だけ
4. **インサートクリップ**（犬が映らない B-roll）を Seedance に移すか。ここは同一性の問題が無いので、安い Kling のままでよい可能性が高い
5. **`generate_audio` は false 固定。** Seedance の既定は true。料金は変わらないが不要な音声が付く
6. 犬種5種での再現性。今回は LALA（イタリアングレーハウンド系）1匹のみ
7. **Preset に返金導線を作るか。** §4.3 で「返金するなら一律 $59 差し引き」は決まったが、
   Preset に返金ボタンを生やすかは別問題。現行は `tier === "custom"` 限定
8. **`REFUND_AMOUNT_USD` の設計。** プラン価格 − $59 の関数にするか、定数2つに割るか

### 見送ったもの

- **今回のテスト3本に `scoreClip` を掛けて毛色ドリフトを数値化する件** — 提案したがオーナーが取り下げ。
  採点は実際の注文で `shotIdentityScores` として残るので、テスト用に別途測る必要はないという判断。
  製品側の同一性ゲート自体は v1 のまま稼働する（これを外す話ではない）。

---

## 6. 再現方法

```bash
npx tsx --env-file=.env scripts/motion-test.ts
```

`scripts/motion-test.ts` に静止画URL・プロンプト・3モデルの入力が全部入っている。
出力は `~/Downloads/marquee-tails-motion-test/`。製品コードは import しているだけで
（`lib/fal-deadline.ts`）、一切変更していない。
