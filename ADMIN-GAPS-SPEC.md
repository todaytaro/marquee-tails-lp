# 管理画面（admin）抜け漏れ監査 ＆ 追加仕様

> **ステータス: 監査のみ / 未実装。** オーナーが日次で使う前提で、現状の admin を監査した結果と追加すべき機能。実装は後日 Sonnet。
> 前提: 管理画面は**既に全面日本語**・Gate 2レビュー・ショット再生成・失敗リトライ・似顔スコア・監査ログまで実装済みで完成度は高い。以下は"運用で困る所"の補完。
> 監査対象: `app/admin/page.tsx`（ダッシュボード）, `app/admin/[orderId]/page.tsx`（注文詳細）, `app/admin/actions.ts`（サーバーアクション）。作成: 2026-07-27

---

## A. 見える化の抜け（ダッシュボード）

現状ダッシュボードが表示するのは **FAILED / AWAITING_ADMIN_APPROVAL（Gate2）/ VIDEO_GENERATING / COMPLETED(直近10)** のみ。

1. **Gate 2 より前の進行中が一切見えない（最重要）**
   - `UPLOADING`（**決済済みだが写真未アップ**）, `IMAGE_GENERATING`, `AWAITING_CUSTOMER_APPROVAL`（**Gate 1・顧客が絵コンテ承認待ち**）が**どこにも出ない**。
   - 影響: 「払ったのに写真を送ってこない客」「絵コンテ承認で止まってる客」が**不可視**＝催促もフォローもできない。滞留＝返金/クレーム/機会損失の温床。
   - 対応: 「**顧客待ち（進行中）**」セクションを追加。UPLOADING / IMAGE_GENERATING / AWAITING_CUSTOMER_APPROVAL を、経過時間（滞留日数）付きで一覧。長期滞留はバッジ表示。

2. **検索・照会が無い**
   - メール / ペット名 / 注文ID / Stripeセッション で引けない。
   - 影響: 顧客から「私の注文どうなってる?」の問い合わせに即答できない。
   - 対応: ダッシュボード上部に**検索ボックス**（部分一致：email / petName / id / stripeSessionId）。

3. **物理配送・POD の状況が全く出ない**
   - schema にある `shippingName/Line1/2/City/Region/PostalCode/Country`, `podOrderId`, `podStatus` が**admin のどこにも表示されない**。
   - 影響: 物理商品（現行 Feature/Collector、v2ではアドオン）の**発送状況の追跡・トラブル対応・Printify注文の突合**ができない。
   - 対応: 注文詳細に「**配送 / POD**」セクション（宛先・`podOrderId`・`podStatus`）。Printify注文への導線（IDコピー）も。

4. **決済額・プランが出ない**
   - `amountPaidCents`, `tier`(=プラン) が詳細にもリストにも無い。
   - 影響: 「いくら払ったか」「どのプランか」が一目で分からない。
   - 対応: 詳細の「注文情報」に **プラン**・**決済額** を追加。リスト行にもプランのバッジ。

5. **日次スループットが見えない**
   - 「1日5本」枠に対する当日の制作数、決済済み仕掛かり数のサマリが無い。
   - 対応（軽め）: ヘッダに「本日の制作 n/5・仕掛かり m件」程度のカウンタ。

---

## B. アクションの抜け

現状のサーバーアクションは **承認(Gate2) / ショット再生成 / 失敗リトライ** の3つ（`actions.ts`）。

6. **メール再送が無い（高価値）**
   - 導線はメールのマジックリンク（`approveToken`）依存。顧客がメールを紛失/迷惑メール行きだと**詰む**。
   - 対応: 詳細に「**顧客用リンクをコピー**」＋「**案内メールを再送**」（アップロード案内 / Gate1承認 / 納品、状態に応じて）。まず"リンクコピー"だけでも実用的。

7. **返金アクションが無い**（現状Stripe手動）
   - 運用は「Stripeダッシュボードから手動返金」。v2の Director's Cut $200返金も当面これ。
   - 対応（軽め）: 詳細に「**Stripeで返金**」ディープリンク（`https://dashboard.stripe.com/.../payments/{intent}` 等）だけでも置くと手数が減る。全自動返金は任意。

8. **管理メモの追記/編集が単独でできない**
   - `adminNote` は Gate 2 承認時にしか付けられない（`approveVideoAction`）。表示は詳細にあるが、**進行中の注文に後からメモできない**。
   - 対応: 「**メモを保存**」の単独アクション（状態を問わず追記可）。

9. **滞留注文の手当てが無い**（任意・エッジ）
   - 例: 決済済みだが顧客が離脱、ペット変更希望 等の手動対応口が無い。ステートマシンのコアは壊さない範囲で、管理メモ＋Stripe手動で運用する前提を明記でも可。

---

## C. v2（新価格・カスタム）で admin に必要になるもの（Pass 2 / Phase B と同時に）

10. **アドオン注文の可視化**（Pass 2）: 誰が Printed Poster / Gallery Canvas を追加購入したか、その Printify 発送状況・宛先（＝A-3 と一体）。デジタルポスターは同梱なので区別表示。
11. **Director's Cut 用の表示**（Phase B）:
    - `customBrief`（顧客の自由記述）, Claude生成の**トリートメント/台本**（企画書）, `treatmentApproved`（文章承認済みか）, `storyboardRerollCount`（**無料リロール X/3**）, 返金適格フラグ。
    - 目的: カスタム注文の進行監視・必要時の人手介入・返金判断。
12. **プラン種別バッジ**（preset / custom）を一覧・詳細に。

---

## D. 言語・UX 微調整（軽微）

13. **ステータスチップが生 enum 英語**（例 `AWAITING_ADMIN_APPROVAL` がそのまま表示）→ 日本語ラベルにマップ（例「Gate2 レビュー待ち」）。それ以外は全面日本語で問題なし。

---

## 優先度（実装順の提案）

- **すぐ（v2非依存・日次運用がすぐ楽になる）**: A-1（Gate2前の可視化）/ A-2（検索）/ B-6（リンクコピー＋メール再送）/ A-3（配送・POD表示）。
- **中**: A-4（プラン・決済額）/ B-7（返金ディープリンク）/ B-8（メモ単独保存）/ D-13（ステータス日本語化）。
- **v2 と同時**: C-10〜12（アドオン可視化・Director's Cut表示・プランバッジ）。

## 制約（実装時）
- **`transitionOrder`（`lib/orders.ts`）が唯一の status 書き込み口**。2ゲートのコアは壊さない。新アクションもここを通す（メモ保存・リンクコピー等は status を変えないので独立）。
- 既存の e2e（`scripts/e2e-state-machine.ts`）を緑のまま維持。
- admin API は `x-admin-secret`、画面はセッション認証（`lib/admin-session.ts`）— 既存方式を踏襲。
