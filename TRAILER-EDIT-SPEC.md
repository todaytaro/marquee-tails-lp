# 予告編エンジン v2 仕様書 — ビート編集・カード・インサート・音響

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-07-29
> 対象: `lib/film-pipeline.ts`（assemble周り中心）、`lib/film-script.ts`、`lib/claude-script.ts`（任意項目の追加のみ）
> **スキーマ変更なし**（新データはすべて `filmArtifacts` Json 内に収める）

---

## 0. 背景 — 「安っぽい」の正体（オーナー実写レビューより）

初の本番 Director's Cut 納品で判明した品質問題:

1. **8秒×6カットの垂れ流し構造そのものが安い。** i2v（画像→動画）は微細な動きしか
   安全に出せない（大きく動かすと本人が崩れる）。微細な動きを8秒間見せれば
   「雑なGIF」に見えるのは当然。**本物の予告編は1カット1.5〜3秒**で、疾走感は
   カット割り・音・文字から生まれ、1カット内の動きからは生まれない。
2. **動きの改善はスコア上限に阻まれる。** クリップの本人度は元の静止画の本人度が
   上限（reanimate で実証: ヨー排除＋cfg 0.55＋Kling pro でもショット3は60のまま）。
   素材側の改善余地は小さい。**編集側で戦うのが正しい。**
3. **音が音楽だけ**＝安い。予告編の体感品質はSFX（ブーム・ライザー・ウーッシュ）が
   支配する。
4. 字幕の焼き込み（映像上のドロップテキスト）は高級感が出ない上に、フォントの
   自動縮小など複雑さの温床。**黒カードで独立して見せる方が予告編の文法**。

**方針: 素材（Kling）を良くする投資をやめ、同じ素材から「本物の予告編構造」を
組む編集エンジンに作り替える。結果としてKling費用は下がる（§7）。**

---

## 1. 成果物の構造 — ビートEDL（Edit Decision List）

`assemble()` を「クリップを順に連結」から「**EDL（ビート列）を組んで刻む**」方式に
書き替える。ビートは3種類:

| 種類 | 中身 | 長さの目安 |
|---|---|---|
| `clip` | Klingクリップの**一部区間**（トリム）。`punchIn` 倍率つき | 2.0〜3.5s |
| `card` | 黒カード＋テキスト（既存 `titleCard()` を流用） | 1.8〜2.2s |
| `insert` | **犬が映らない**情景スチル＋Ken Burns（§4） | 2.0s |

### 1.1 パンチイン（同じ素材から2ビート作る）

1本のクリップから (a) 素のフレーミング (b) **中央クロップ1.3〜1.4倍に拡大**した
「寄り」を作る。**同じ時間範囲を再利用してよい**（サイズが変わればカットとして
成立する — 実写編集の常套手段）。ffmpeg: `crop=iw/Z:ih/Z,scale=1920:1080`。
1080p素材の1.4倍クロップは実効770p→軽いシャープで許容（予告編はカットが短いので
目立たない）。Z上限は1.5。

### 1.2 既定のEDLテンプレート（6クリップ＋インサート3枚＋カード）

```
open   card   "MARQUEE TAILS PRESENTS"      2.0s
b1     clip0  wide                           3.0s
c1     card   loglines.intro                 2.0s
b2     clip0  punch-in 1.35x                 2.0s
b3     clip1  wide                           2.5s
c2     card   "STARRING" + petName           2.2s
b4     clip1  punch-in                       2.0s
i1     insert #1 (Ken Burns)                 2.0s
b5     clip2  wide                           2.5s
b6     clip2  punch-in                       2.0s
c3     card   loglines.turn                  2.0s
b7     clip3  wide                           2.5s
i2     insert #2                             2.0s
b8     clip3  punch-in                       2.0s
b9     clip4  wide                           2.5s
c4     card   loglines.rise                  2.0s
b10    clip4  punch-in                       2.0s
i3     insert #3                             2.0s
b11    clip5  wide                           2.5s
b12    clip5  punch-in（クライマックス長め）   3.5s
c5     card   petName（大）+ loglines.tagline 3.0s
c6     card   "COMING SOON"                  2.0s
c7     card   "A MARQUEE TAILS FILM"         1.5s
```

素の合計 ≈ 51.7s。

### 1.3 60秒保証（正規化）

**合計が正確に60.0秒になるよう、`clip`/`insert` ビートの長さを比例スケールする**
（カードは固定。カードを伸ばすと間延びするため）。
`scale = (60 - Σcard秒) / Σ(clip+insert素秒)` を各ビートに掛ける。
上のテンプレートでは scale ≈ 1.24 → clip ビートが 2.5s→3.1s 程度に伸びる。
インサートが用意できない注文（§4.4 フォールバック）は clip ビートだけで
正規化されるので、**どのケースでも尺は60秒ちょうど**になる。
実装は端数をフレーム単位（1/30s）に丸め、丸め誤差は最終ビートで吸収する。

- EDLは**データ（配列リテラル）として定義**し、順序・重み調整が1箇所の編集で
  済むようにする。
- 決定性: EDL構成に乱数を使わない（再アセンブルで同じ映画になること。
  `getShotMotion` と同じ理由）。

### 1.4 字幕焼き込みの廃止

`normaliseClip()` の drawtext キャプション（intro/STARRING/turn/rise の焼き込み）は
**廃止**し、すべて §1.2 のカードに移す。効果:
- 高級感（黒カード＋Bebasの全面テキスト）
- フォント自動縮小・JPフォント分岐などの複雑さが**カードにのみ**残る
  （`titleCard()` は既存のまま流用、petName は従来どおり `FONT_NAME`）
- 映像は常にクリーン → パンチインでテキストが切れる事故も構造的に消える

---

## 2. 音響設計（体感品質の最大レバー）

### 2.1 SFXアセット

`public/sfx/` に**同梱ファイル**として置く（注文ごとの生成はしない）:

| ファイル | 用途 |
|---|---|
| `boom.wav` | カードの表示瞬間（ドン） |
| `riser.wav` | クライマックス直前（b11〜b12へ向けて） |
| `whoosh.wav` | clip→clip の主要な切替（全部には付けない。c1/c3/c4 前後など5〜6箇所） |

**入手**: オーナーが用意（フリー音源 or Stable Audio で1回だけ生成して保存）。
実装は「ファイルが無ければSFXなしで従来どおり合成」のフォールバック必須
（＝アセット未配置でもパイプラインは壊れない）。

### 2.2 ミキシング

ffmpeg `amix`/`adelay` で音楽トラックに重ねる。
- 音楽: 既存 `generateScore()` のまま。カードの `boom` 時に音楽を **-2〜-3dB
  ダッキング**（`sidechaincompress` は過剰。`volume` を区間指定で下げる単純実装で可）
- SFX音量は音楽より上（boom: 0dB、whoosh: -6dB 目安、定数化）
- 60秒に**音楽をフェードアウト**（最後1.5s）— 現状の切りっぱなしを直す

---

## 3. 仕上げ（グレード）

`normaliseClip` 相当の後段に共通フィルタチェーンを追加:

1. **2.35:1 レターボックス**: 16:9 フレーム内で上下に黒マット
   （`drawbox` 2本 or `crop=iw:iw/2.35` + `pad`。**カードも同じマット**を敷いて統一）
2. **フィルムグレイン**: `noise=alls=6:allf=t+u` 程度（強すぎ注意、定数化）
3. **簡易グレード**: `eq=saturation=1.06:contrast=1.04` ＋
   `colorbalance` でシャドウ寄りに僅かに青（ティール＆オレンジの気配。やり過ぎ禁止）
4. **ビネット**: `vignette=PI/5` 弱め

すべて定数として1箇所にまとめ、コメントで「無効化はここ」と明記。

### 3.1 9:16 ソーシャル版

同じEDLで再アセンブル。相違点: 2.35:1マットは**適用しない**（縦動画に横マットは
不成立）。中央クロップは現行踏襲。カードは縦用レイアウト（`titleCard()` に
解像度引数を追加 or 縦専用関数）。グレイン・グレードは共通。

---

## 4. インサートカット（犬が映らないカット）※オーナー確定事項

**世界観の情景だけのカット**（例: 雨上がりのネオン看板、自販機の灯り、濡れた
横断歩道、ウイスキーグラスのアップ、走り去る車のテールランプ）。

### 4.1 なぜ承認不要か（確定した設計判断）

- **犬が映らない＝本人性リスクがゼロ。** Gate 1 が守っているのは「うちの子に
  見えるか」であり、情景カットには守るものがない
- 物語は Gate 0（トリートメント: custom）／ワールド選択（preset）で承認済み。
  インサートはその世界観の**装飾**であって新要素ではない
- よって **Gate 1（絵コンテ承認）には含めない。動画生成時に自動生成して差し込む**
- Gate 2（admin品質確認）では見えるようにする（§4.5）

### 4.2 生成方法 — Kling不使用（コストゼロに近い）

- **nano-banana でスチル3枚**を生成（動画生成時、`runFilmGeneration` 冒頭）
- 動きは **Ken Burns**（ffmpeg `zoompan`: ゆっくりズームイン/アウト＋僅かなパン）。
  2秒のインサートに i2v は不要。スチル＋ズームで映画のBロールとして十分成立し、
  **Klingコストが1円もかからない**
- プロンプト: 「(世界の雰囲気描写), cinematic still, no animals, no people,
  moody lighting, 16:9」＋ネガティブに動物・人物
- `filmArtifacts.insertStillUrls: string[]` にキャッシュ（再開・再アセンブルで再利用。
  既存の `FilmArtifacts` 型に任意フィールド追加 — Json内なのでマイグレーション不要）

### 4.3 インサートの題材はどこから来るか

- **preset**: `lib/film-script.ts` に `WORLD_INSERTS: Record<string, string[]>` を
  新設（world毎に5個程度の情景プール、注文IDの stableHash で3個選択 — 決定性）
- **custom**: `lib/claude-script.ts` の `submit_treatment` ツールに
  **任意フィールド `inserts: string[]`**（3個、英語、動物・人物なし）を追加。
  システムプロンプトに「世界観を彩る、ペットの映らない情景カットを3つ」と1行追加
- **フォールバック**（旧注文・Claudeが返さなかった場合）: custom で `inserts` が
  無ければ**インサートなしのEDL**で組む（§1.3 の正規化で60秒は維持される）。
  絶対に落とさない

### 4.4 本人判定ゲートとの関係

インサートは**スコアリング対象外**（犬が映らないので当然）。
`shotIdentityScores` は従来どおり6クリップ分のみ。混同しないよう、インサートは
`clipUrls` とは別のフィールドに保持し、既存の採点ループに触らせない。

### 4.5 admin表示

`app/admin/[orderId]/page.tsx` に「インサート（情景カット）」セクションを追加
（スチル3枚の読み取り表示のみ。差し替え操作はv2以降）。

---

## 5. Kling 尺の短縮: 8s → 5s

ビートは最長3.5s＋パンチインは同区間再利用可なので、**5秒素材で全ビートを賄える**。
- `SHOT_SECONDS = 5` に変更（Kling duration enum は 3〜15s なので合法）
- **旧注文の8sクリップとの互換**: EDLはトリムして使うので混在しても壊れない
  （再アセンブル・ショット再生成の互換を保つこと）

---

## 6. 触るファイルまとめ

| ファイル | 変更 |
|---|---|
| `lib/film-pipeline.ts` | `assemble()` をEDLベースに全面改修（§1）。SFXミックス（§2）。グレード（§3）。`SHOT_SECONDS` 5s。インサート生成＋`filmArtifacts.insertStillUrls`（§4.2）。`normaliseClip` の焼き込みキャプション廃止（§1.4）。9:16も同構造（§3.1） |
| `lib/film-script.ts` | `WORLD_INSERTS` 追加（§4.3）。`TITLE_CARDS` は流用 |
| `lib/claude-script.ts` | ツールschemaに任意 `inserts` 追加＋プロンプト1行（§4.3）。**必須にしない**（後方互換） |
| `app/admin/[orderId]/page.tsx` | インサート表示セクション（§4.5） |
| `public/sfx/` | SFX 3ファイル（オーナー用意。無ければスキップ） |
| `.env.example` | 変更不要（新envなし） |

**変更しないもの**: スキーマ／状態機械／Gate 1・Gate 2 の承認フロー／ポスター
パイプライン／`getShotMotion`（v1のまま）／`KLING_MODEL` env機構。

---

## 7. コスト影響（概算）

| 項目 | Before | After |
|---|---|---|
| Kling | 8s × 6本 | **5s × 6本（-37.5%）** |
| インサート | — | nano-banana スチル3枚（数円） |
| SFX | — | 同梱ファイル（ランニング0円） |
| ffmpeg | 合成1回 | ビート数は増えるが large-1x で数分・数円 |

**品質を上げる変更が、そのまま動画生成費の削減になる。**
（Kling pro/standard の選択は本仕様の範囲外 — `KLING_MODEL` env で継続A/B。
本人度は静止画上限で頭打ちのため、proの価値は「動きの質」のみ。fal請求明細を
見て判断する）

---

## 8. 検証（実装者向け）

静的: `tsc` / `eslint` / `next build`。
**ローカル実測**（DBに触らない範囲で）: サンプルmp4 6本＋スチル3枚を引数に
`assemble()` 相当を直接呼ぶ検証スクリプト（`scripts/test-assemble.ts` 新設可）で
(1) 尺がちょうど60.0s（±1フレーム）、(2) カード・インサート・パンチインの出現順、
(3) SFXファイル欠如時のフォールバック、を確認して報告。
ffmpeg は `ffmpeg-static` でローカル実行可能。fal・DB・Trigger.dev は使わない。

## 9. 明示的にやらないこと（v2以降）

- インサートの admin差し替え／顧客選択
- start+end フレーム補間（Kling tail_image）— 動きの本命だが別仕様で
- モデル混成（Veo等をヒーローショットに）— `KLING_MODEL` envで実験してから
- 9:16専用の縦構図インサート
