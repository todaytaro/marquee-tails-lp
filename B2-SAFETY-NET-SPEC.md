# B2 — Director's Cut 安全網 仕様書（リロール3回 ＋ $200返金導線）

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-07-31
> 対象は **Director's Cut（custom, $249）のみ**。Preset（$99）は対象外（§7）。
> 出典: `PRICING-PRODUCT-V2-SPEC.md` §3.5(C)。B1 の実装ブリーフが明示的に
> B2 へ繰り延べた3項目のうち、**ウォーターマークは実装済み（現在は無効化）**
> なので、本仕様は残る2項目を扱う。

---

## 0. なぜ必要か

$249 を先払いさせる商品で、顧客の最大の恐怖は「**AIガチャに先払いする**」こと。
仕様書 §3.5(C) はこう謳うと決めている:

> まず何度でも直します（文章は潤沢、絵コンテのリロールは3回まで無料）。
> それでも撮影前に承認できなければ **$200 を返金**。
> Concept & Storyboard fee **$49 は非返金**。

**この約束は現在ひとつも実装されていない。** 謳っていない（価格ページにも
未記載）ので嘘はついていないが、**恐怖を解く仕掛けが無いまま $249 を売っている**
状態。B2 はこの約束を実際に果たせるようにする。

---

## 1. 用語の確定（ここが曖昧だと機能が重複する）

「やり直し」は**3つ**あり、別物として扱う。

| # | 名称 | 何をやり直すか | 誰が | 現状 |
|---|---|---|---|---|
| 1 | **トリートメント修正**（Gate 0） | Claudeが**文章**を書き直す | 顧客 | ✅ 実装済み（`treatmentRevisionCount`、内部上限20＝実質無制限） |
| 2 | **絵コンテのリロール**（Gate 1） | **1カットの3テイク**を引き直す | 顧客 | ❌ **本仕様で新設** |
| 3 | ショット再生成（Gate 2） | 動画1本／静止画から | **管理者** | ✅ 実装済み |

**仕様書が「3回まで無料」と言っているのは #2。**

### 1.1 リロールの定義（確定）

> **リロール** = Gate 1 で、**1カットの3テイクを引き直す**こと。
> **注文あたり3回まで無料。顧客は指示を書けない**（シードを変えて引き直すだけ）。

**なぜ1カット単位か**: 顧客の不満は「カット3の顔が変」のように局所的。
全6カット（18枚 ≈ $4）を1回とすると3回で$12＝$249の5%が飛ぶ上、
「1カットだけ直したい」に応えられない。1カット（3枚 ≈ $0.7）なら3回で$2.1、
かつ「2・4・5が微妙」に全部対応できる。

**なぜ指示なしか**: Gate 1 は「3枚から選ぶ」画面であり、「気に入らない→引き直す」
が自然な操作。文章で直したい要望は Gate 0 の修正ループ（実質無制限）が担当。
両方に指示欄を置くと役割が重複し、どちらも中途半端になる。

**カウント単位**: どのカットに使っても**注文全体で3回**。

---

## 2. スキーマ変更（マイグレーションは**適用しない**）

`Order` に3列追加。**実装者はマイグレーションSQLを書くだけで、絶対に適用しない**
（`.env` の DATABASE_URL は本番 Supabase を指している）。オーナーが手動で適用する。

```prisma
storyboardRerollCount Int      @default(0) // Gate 1 の無料リロール消費数（上限 STORYBOARD_REROLL_CAP）
refundRequestedAt     DateTime?            // 顧客が $200 返金を要求した時刻
refundIssuedAt        DateTime?            // 管理者がStripeで返金を実行し、記録した時刻
```

- マイグレーション名は既存の慣習に合わせる（`prisma/migrations/2026…_b2_safety_net/`）
- **OrderStatus は増やさない**。返金は「注文の属性」であって新しい工程ではない。
  返金完了後の終端は既存の `CANCELLED` を使う（§4.3）

---

## 3. リロール（Gate 1）

### 3.1 API

`POST /api/orders/reroll-cut` — `{ approveToken, cutIndex }`

ガード（すべて満たさないと 4xx、**理由を返す**）:
- 注文が `AWAITING_CUSTOMER_APPROVAL`（Gate 1 の最中）であること
- `tier === "custom"`（Preset は対象外、§7）
- `storyboardRerollCount < STORYBOARD_REROLL_CAP`（= 3）
- `cutIndex` が 0..5

処理:
1. `storyboardRerollCount` を**先に**インクリメント（二重押し・並行リクエストで
   3回を超えないよう、`updateMany` の条件付き更新で原子的に。既存の
   addon webhook が使っている作法と同じ）
2. そのカットの**3テイクを再生成**（`lib/stills-pipeline.ts` の
   `generateGatedTake` を流用。**シードは既存と必ず変える** — 同じ絵が返ると
   リロールを1回消費した意味がない）
3. `storyboardOptions` の当該カットだけ差し替えて保存
4. 顧客の選択（`chosenStills`）は Gate 1 承認時にまとめて確定するので、
   ここでは触らない

**所要時間**: 3枚生成で30〜60秒。**同期実行で返すか、Trigger.dev に投げるかは
実装者判断**。同期にする場合は Vercel の関数タイムアウトに収まることを確認する
（B1 の `revise-treatment` が同期でClaudeを呼んでいる前例あり）。

### 3.2 UI（`components/StoryboardWizard.tsx`）

各カットの3テイクの下に「**この3枚が気に入らない → 引き直す（残りN回）**」。

- 残り回数を**常に表示**する。「3回まで無料」は売り文句なので、隠すと価値が伝わらない
- **0回になったら**ボタンを消し、代わりに §4 の返金導線を出す
- 引き直し中はローディング（既存の `ProductionProgress` の作法に倣う）
- 押した直後に**取り消せない**ことを明示（1回消費するため）

### 3.3 admin 表示

注文詳細に `storyboardRerollCount` を「リロール N/3 回使用」として出す。
返金要求が来たときの判断材料になる。

---

## 4. $200返金導線

### 4.1 発動条件（仕様書 §3.5(C) の「ハイブリッド」）

**リロール3回を使い切り、かつ まだ Gate 1 を承認していない**ときのみ、
返金を**要求できる**。

- 3回未使用のうちは**返金要求できない**（「まず直す」が先）
- **動画レンダリング前**に限る。Gate 1 承認後（＝撮影開始後）は受注生産につき返金不可
- 金額: **$200 返金 / $49 非返金**（Concept & Storyboard fee）

### 4.2 顧客側

Gate 1 で3回使い切ったときに出す。文言は**謝罪ではなく契約の履行**として書く:

> それでも思うようにならなければ、撮影前にここで終わりにできます。
> 制作費 $200 を返金します（企画・絵コンテ費 $49 は返金対象外 — 
> あなたのために実際に作った企画書と絵コンテはお手元に残ります）。

- ボタンは**目立たせない**（存在は明示するが、リロールより主張しない）
- 押すと**確認ステップ**を挟む（不可逆であることを明示）
- 確定すると `refundRequestedAt` を記録し、**それ以上 Gate 1 を進められなくする**

### 4.3 管理者側（返金の実行は**手動**）

仕様書 §3.5 が「当面はStripeダッシュボードから手動部分返金で運用可」と明記して
いるので、**Stripe APIは呼ばない**。

admin 注文詳細に:
- 「**返金要求あり**」の警告表示（`refundRequestedAt`）＋ Stripe セッションIDの
  コピーボタン（既存の `CopyLinkButton` を流用）
- 「**$200 返金済みとして記録**」ボタン → `refundIssuedAt` を記録し、
  注文を `CANCELLED` へ遷移
- **返金額をアプリ側で計算しない**。金額はStripe画面で人が入力する。
  ここで計算して表示すると「アプリが返金した」ように見え、実態とズレる

### 4.4 メール

- 顧客が返金要求 → **管理者に通知**（既存の `lib/mocks.ts` の送信経路を流用）
- 管理者が返金記録 → **顧客に確認メール**（「$200 を返金しました。反映まで
  5〜10営業日」）

---

## 5. 開示（仕様書 §3.5 が「購入前に明示」を必須としている）

非返金フィー＋部分返金は、**購入前に開示していれば**一般に許容される運用。
5箇所すべてに書く:

1. **価格ページ**（`components/PricingTeaser.tsx`）— Director's Cut の箇条書きに
   「絵コンテのリロール3回無料。撮影前なら $200 返金（$49 は企画費）」
2. **チェックアウト同意文**（`app/api/checkout/route.ts` の `custom_text`）—
   custom の場合のみ1行追加
3. **利用規約**（`app/(legal)/terms/page.tsx`）
4. **返金ポリシー**（`app/(legal)/refund/page.tsx`）— 既存の「不備は2回まで無償
   再制作、その後全額返金」と**矛盾しない**書き分けが必要。あちらは
   **納品物の不備**、こちらは**制作前の方向性不一致**
5. **特商法**（`app/(legal)/tokushoho/page.tsx`）の返品・キャンセル欄

**最終更新日**も更新する。

---

## 6. 触るファイル

| ファイル | 変更 |
|---|---|
| `prisma/schema.prisma` ＋ 新規マイグレーション | §2（**適用しない**） |
| `app/api/orders/reroll-cut/route.ts` | 新規（§3.1） |
| `app/api/orders/request-refund/route.ts` | 新規（§4.2） |
| `components/StoryboardWizard.tsx` | リロールボタン＋残回数＋返金導線（§3.2/§4.2） |
| `lib/stills-pipeline.ts` | 1カット再生成の関数を切り出し／流用（§3.1） |
| `app/admin/[orderId]/page.tsx` ＋ `app/admin/actions.ts` | リロール数表示、返金要求表示、返金記録ボタン（§3.3/§4.3） |
| `lib/mocks.ts` | 通知メール2種（§4.4） |
| `components/PricingTeaser.tsx`／`app/(legal)/*`／`app/api/checkout/route.ts` | 開示（§5） |

**変更しないもの**: 動画パイプライン（EDL・エンコード・start+end・SFX）、
ポスターパイプライン、Gate 2 のショット再生成、Preset の挙動、
`treatmentRevisionCount` の既存挙動。

---

## 7. Preset（$99）は対象外

Preset には Gate 0（トリートメント）が無く、$49/$200 の内訳も存在しない。
リロールも返金導線も**出さない**。実装は `tier === "custom"` で分岐し、
Preset の Gate 1 は現状のままであることをテストで確認すること。

---

## 8. 検証

**DBに接続しない**（本番Supabase）。マイグレーションは**書くだけ**。

- `tsc` / `eslint` / `next build`
- **ガードのテスト**（DB不要な純関数に切り出して検証、またはコード読解で証明）:
  1. 4回目のリロールが拒否される
  2. 3回未使用で返金要求が拒否される
  3. Gate 1 承認後に返金要求が拒否される
  4. Preset 注文でどちらも拒否される
  5. 同時に2回押しても `storyboardRerollCount` が3を超えない
- `scripts/test-assemble.ts` は**無関係だが壊さない**こと（実行して確認）

---

## 9. やらないこと

- **Stripe API での自動返金**（仕様書が手動運用でよいと明記）
- 納品後の再制作導線（返金ポリシーの「2回まで無償再制作」は現状メール運用。
  §0 の約束とは別物で、件数が出てから作る）
- ウォーターマークの再有効化（実装済み・無効中。返金導線が入った時点で
  `WATERMARK_PREVIEWS_ENABLED = true` に戻す判断を別途行う）
