# 絵コンテを admin が承認してから顧客に出す 仕様書

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-08-01
> 前提: `LORA-STORYBOARD-SPEC.md`（実装・デプロイ済み）。本書はその直後の運用変更。

---

## 0. なぜ

初回の本番 Preset 注文で、顧客に出す18枚の中に**犬がどこにいるか読めないテイク**が混ざった
（noir/playful のカット1、トレンチコートに埋もれた1枚）。

カット文側の原因は `a9dc6c4` で潰した（動きを静止画に書かない／顔の前に物を置かない）。
**しかしそれは「この種の事故が二度と起きない」ことを意味しない。**

- **解剖ゲートは止められない。** 脚の本数を見る仕組みなので、布の塊には数える脚が無い
- $99〜$249 の商品で、選択肢3つのうち1つが破綻していると、**実質2択に減る**だけでなく
  「出力を確認していない会社」という印象を与える
- Director's Cut のリロールは「顧客が納得できなければ $200 返金」という約束の一部。
  **こちらの品質不良でその枠を消費させるのは筋が違う**

**オーナー判断: 顧客に出す前に人間が見る。** 決済は前払いで済んでおり、
速度より品質を取る余地がある。

---

## 1. 決定事項（オーナー承認済み）

| 論点 | 決定 |
|---|---|
| admin は承認/却下だけか | **カット単位で引き直せる**。1枚のために18枚作り直すのは無駄 |
| 対象プラン | **両方**（Preset / Director's Cut） |
| admin への通知 | **送る。** 確認待ちが発生したらオーナーにメール |
| 顧客への告知 | 「3時間以内」→ **「1営業日以内」** |

---

## 2. 状態の持ち方（新しい OrderStatus を作らない）

**絵コンテが完成しても `IMAGE_GENERATING` のまま止める。**

- 顧客の待機画面は**そのままで正しい**。実際まだ制作中（人間の確認中）なので嘘にならない
- **確認待ちキュー = `status === IMAGE_GENERATING` かつ `storyboardOptions` が埋まっている注文**
- admin が承認した時点で `AWAITING_CUSTOMER_APPROVAL` に遷移し、顧客メールを送る

新しい enum 値も、顧客側の新しい画面も要らない。**今ある自動遷移を、ボタン1つに置き換えるだけ。**

> **ただしスキーマ変更は1つ必要**（§4）。「変更ゼロ」ではない。

---

## 3. 変更内容

### 3.1 `completeStillsGeneration`（`lib/stills-pipeline.ts`）

現状: storyboard を保存 → `AWAITING_CUSTOMER_APPROVAL` に遷移 → **顧客に**メール。

変更後:

1. storyboard を保存する（現状のまま）
2. **遷移しない**
3. **顧客にメールを送らない**
4. 代わりに**オーナーに確認依頼メールを送る**（§3.4）

`sendChooseStillEmail` の try/catch は現状のまま維持すること
（メールの失敗で18枚を作り直さないための措置。理由はコード内コメント参照）。
新しい admin 通知も同じく非致命にする。

### 3.2 admin の絵コンテ確認画面（`app/admin/[orderId]/page.tsx`）

**現状 admin は絵コンテを表示していない**（リロール回数の数字だけ）。新規に作る。

- 6カット × 3テイクを表示する。顧客の Gate 1 と同じ並び順
- **カットごとに「このカットを引き直す」**
- **「承認して顧客に送る」**（全カット確認後）

顧客に見せる前の画面なので、透かしの有無や画像の出し分けは顧客側と揃える必要はない。
既存の `storyboardOptions` をそのまま読めばよい。

### 3.3 admin のリロール（`rerollCutTakes` の再利用）

既存の `rerollCutTakes` をそのまま使う。**ただし2点、必ず分離すること。**

**(a) 顧客のリロール枠を消費しないこと。**
`storyboardRerollCount` は Director's Cut の B2 安全網（3回まで無償、
使い切ってなお不満なら $200 返金）のカウンタ。**admin の引き直しでこれを増やしてはならない。**
約束の残量が、こちらの品質不良で目減りする。

**(b) シードが顧客のリロールと衝突しないこと。**
`rerollSeedBase` は `STILL_SEED + cutIndex*100 + REROLL_SEED_BAND*rerollCount` で、
リロール回数ごとに帯を分けて同じ絵を二度引かないようにしている。
admin のリロールが**別のカウンタで同じ計算をすると、顧客のリロールと同じ種になりうる。**
`adminRerollCount` 用に**別の帯**を割り当てること（例: 顧客帯と重ならないオフセットを足す）。
「たまたま被らない」ではなく、**構造として被り得ないこと**を証明できる形にする。

### 3.4 オーナーへの確認依頼メール（`lib/mocks.ts`）

`sendRefundRequestedAlert` と同じ形で新設する（宛先 `ADMIN_ALERT_EMAIL`、
未設定なら `support@marqueetails.com`、本文に `/admin/<orderId>` へのリンク）。

**これは「あれば良い」ではなく実装の一部。** この変更でオーナーがボトルネックになる以上、
**オーナーが気づく仕組みまで揃って初めて機能する。** 通知が無ければ、
admin を定期的に見に行かない限り顧客は待ち続ける。

### 3.5 admin の承認アクション（`app/admin/actions.ts`）

新設。`IMAGE_GENERATING` → `AWAITING_CUSTOMER_APPROVAL` に遷移し、
`sendChooseStillEmail` を送る。既存の `approveVideoAction`（Gate 2）と同じ姿勢で書く。

**遷移の前提条件を必ず検査すること**: 対象注文が `IMAGE_GENERATING` であり、
`storyboardOptions` が6カット揃っていること。生成途中の注文を承認できてはならない。

### 3.6 顧客向け文言（3箇所）

「約3時間」を**「1営業日以内」**に変える。

| 場所 | 現状 |
|---|---|
| `components/PhotoUploadForm.tsx` | 送信ボタン下の次ステップ説明（プラン別に2文） |
| `lib/mocks.ts` `sendWelcomeUploadEmail` | 「Building your storyboard takes up to about three hours.」 |
| `app/approve/[token]/page.tsx` `WaitingView` | 「That takes up to about three hours.」 |

**「約3時間」は実測52分に対する余裕として置いた数字だった。**
人間の確認が入る以上その根拠は消える。**守れる約束に変えること** —
夜間に届いた注文は翌日になる前提で書く。

理由の説明は残す（この子専用のモデルを学習しているから時間がかかる）。
**待たせる理由が商品価値そのものである、という説明は今回も有効。**
そこに「director が全カットに目を通す」を足すと、
**遅さが手間の証明に変わる**ので、むしろ売りになる。

---

## 4. スキーマ変更

`Order` に **`adminRerollCount Int @default(0)`** を追加（§3.3(b) のシード分離用）。

マイグレーションSQLファイルは生成するが**適用しない**（オーナーが手で流す）。
既存行は 0 で問題ない。

---

## 5. 触るファイル

| ファイル | 変更 |
|---|---|
| `lib/stills-pipeline.ts` | `completeStillsGeneration` を遷移させない（§3.1）、admin リロールのシード帯（§3.3(b)） |
| `lib/mocks.ts` | オーナー向け確認依頼メール（§3.4）、ウェルカムメールの文言（§3.6） |
| `app/admin/[orderId]/page.tsx` | 絵コンテ確認UI（§3.2） |
| `app/admin/actions.ts` | 承認アクション、admin リロールアクション（§3.3/§3.5） |
| `app/approve/[token]/page.tsx` | 待機画面の文言（§3.6） |
| `components/PhotoUploadForm.tsx` | 次ステップ説明の文言（§3.6） |
| `prisma/schema.prisma` | `adminRerollCount`（§4） |

**変更しないもの**: `OrderStatus` の enum、顧客側の Gate 1 UI、動画パイプライン、
B2 安全網のロジック（`storyboardRerollCount` の意味と上限）、Stripe / Printify。

---

## 6. 検証

**DBに接続しない。** 実測は本番注文でのみ可能。

- `tsc` / `eslint` / `next build`
- `scripts/test-assemble.ts` を壊さないこと（実行はしない。10分以上かかる）
- 証明すべきこと（コード読解で `file:line` を挙げて示す）:
  1. 絵コンテ完成時に顧客へメールが飛ばないこと。飛ぶのはオーナー宛のみ
  2. admin の承認を経ずに `AWAITING_CUSTOMER_APPROVAL` に到達する経路が無いこと
  3. admin のリロールが `storyboardRerollCount` を増やさないこと
  4. admin のリロールのシードが顧客のリロールのシードと**構造的に**衝突し得ないこと
  5. 承認アクションが、生成途中の注文（`storyboardOptions` 未完）を承認できないこと
  6. オーナー宛メールの失敗が注文を落とさないこと

---

## 7. 認識しておくべきこと

**オーナーがボトルネックになる。** 注文が増えれば破綻する設計で、
それは承知の上での判断（返金祭を避ける方が優先）。将来的には
「解剖ゲートを賢くして自動に戻す」が本筋だが、
**今は人間が見る方が確実**という判断を明示的に採用している。

**納品までの時間が延びる。** 実測52分だったものに、確認待ちが乗る。
特商法ページの「絵コンテ承認後48時間以内」（動画の納期）は変わらないが、
**絵コンテ自体の納期を新たに約束することになる**点に注意。
