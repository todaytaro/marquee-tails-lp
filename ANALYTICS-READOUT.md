# 判定日に何を見るか（2026-08-24 時点で**実際に取れるもの**）

`social/ANALYTICS-SPEC.md` は4つの実装（①ビーコン ②新テーブル ③決済経路の記録
④intakeの質問）を前提にしている。**入っているのは①だけ。**

②③④は一度実装したうえで**取り消した。** 理由は技術的な問題ではなく方針で、
オーナーが「サービスのコードと仕様を動かすのは避けたい」と判断した。実際、
あの3つは LP の配信方法（静的→動的）、決済経路、顧客が見るフォームを変える。
壊れないことと、動かさないことは別の話。

このファイルは**その制約下で何が判定できて、何ができないか**を書いたもの。
できないことを書いていない計測メモは、判定日に嘘をつく。

---

## 入っているもの — Cloudflare Web Analytics

`app/layout.tsx` にビーコンのタグ。**`NEXT_PUBLIC_CF_BEACON_TOKEN` が未設定なら
何も出力しない**ので、トークン取得前でも本番に出して安全。

取得と設定の手順は `.env.example` の同名の項目に書いてある。
**`NEXT_PUBLIC_` はビルド時に埋め込まれるので、登録しただけでは効かない。再デプロイが要る。**

見られるのは訪問数・国・参照元・回遊。Cloudflare のダッシュボードで読む。

---

## 判定できるもの

### G1'（9/15）米国比率 — 30%未満なら獲得チャネルとして棄却

Cloudflare の国別内訳で判定できる。**このゲートは執行可能。**

ひとつ注意: **ビーコンは広告ブロッカーに落とされる。** 落ちる割合が国によって
偏るなら比率も歪むが、「日本ばかりで米国が3割に満たない」ような大きな差は
その程度の誤差では埋まらない。**大きく外れているかを見るゲートなので、精度は足りる。**

### 流入元 — UTM を付けたリンク

投稿する URL に必ず付けること。付け忘れた分は**後から遡れない。**

```
https://www.marqueetails.com/?utm_source=instagram&utm_medium=organic&utm_campaign=armA
```

Cloudflare Web Analytics が UTM をどう見せるかは**未確認**（パスやクエリの
扱いはダッシュボードで実物を見て確かめること）。もし分解して見せないなら、
Arm A / Arm B の比較はこの構成では測れない。

---

## 判定できないもの

### G3 / G3'（累積セッション、500セッションで注文0）

**近似しかできない。** 訪問数はビーコン（落ちる）、注文数は DB（落ちない）で、
分母と分子の取りこぼし方が違う。「500あるのに0件だからページか価格の問題」と
言い切るには、分母を信用できる必要がある。

**執行するなら**、サーバー側で着地を数える必要がある（LP を動的にして DB に1行）。
判定日にこのゲートを本当に使うなら、それより前に決める。

### G5（GB 単独の451件数）— 英国を開ける判断

**取れない。** 決済APIが呼ばれた瞬間にしか分からない情報で、いまは
`app/api/checkout/route.ts` の `console.log("[checkout] blocked country=...")` に
出て、**Vercel のログ保持期限で消えている。**

英国を開ける条件の1番目が「451の件数を国別に数える」なので、**この判断は
現構成では下せない。** 決済経路に記録を足すまで、90日経っても材料が無い。

**急がなくてよい理由**: 判断の時期は最短でも90日後。それまでに決めればよく、
今すぐ決済経路を触る必要はない。**ただし「そのうち取れる」ではなく「今は取って
いない」と認識しておくこと** —— 90日後に振り返って数字が無いことに気づくのが
最悪の形。

---

## 自己申告の経路（未実装）

`social/ANALYTICS-SPEC.md` ④ の "How did you find us?"。**n<20 では最も正確な
計測**だが、顧客が見るフォームを変えるので今回は入れていない。

代替: 納品メールへの返信や、`ratingComment`（納品評価の自由記述、既存）に
書かれることがある。**能動的に聞いていない以上、経路が分かるのは偶然。**
