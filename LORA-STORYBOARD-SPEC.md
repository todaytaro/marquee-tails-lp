# 絵コンテ生成を LoRA（B1）に載せ替える 仕様書

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-08-01
> 前提となる調査結果は本書 §1 に全部書いてある。**§1 を読まずに §2 以降を実装しないこと。**
> 先行する `IDENTITY-FIDELITY-SPEC.md`（判定を実写真基準に）は実装・push 済み（`abe6e14`）。
> 本書はその続きで、**測れるようになった似顔を、実際に良くする**ための変更。

---

## 0. 何が決まったか

**絵コンテのテイク生成を、`nano-banana-pro/edit` の参照画像チェーンから、
ペットごとに学習した LoRA を使う `fal-ai/flux-2/lora/edit` の1コール（以下 B1）に置き換える。**

オーナーが camyu（ミニチュアシュナウザー）の実写真と生成物を並べて判断した結果。
スコアではなく目視で決めた。理由は §1。

---

## 1. 調査結果（この判断の根拠。すべて実測）

### 1.1 従来方式は「犬種標準の犬」を作っていた

`scripts/compare-identity.ts` で4方式を同一条件で比較した。

| 方式 | 中身 | 結果 |
|---|---|---|
| CURRENT | 衣装シート1番目・実写真最後（出荷中） | ❌ 整えられた一般的なシュナウザー |
| A/EDIT | nano-banana に実写真を1番目 | ❌ 同上。CURRENT と見分けがつかない |
| B1 | LoRA + 衣装参照を1コール | ✅ camyu |
| B2 | LoRA で裸 → nano-banana が着せる | △ 場合による（§1.2） |

**VLM スコアは A/EDIT を最高評価（83）にしたが、絵は別の犬だった。**
一方 B1 は 77 で camyu だった。**スコアは方式選択の判断に使えない。**
これは `IDENTITY-FIDELITY-SPEC.md` §6 で予告した「モデルの上限」が、
実際にはモデル選択で越えられたことを意味する。

### 1.2 【最重要】2パス方式は原理的に成立しない

`scripts/test-framing.ts` で、nano-banana の着せ工程を3つのプロンプトで検証した。

| | 構図 | 似顔 |
|---|---|---|
| bridge V0（対照） | 変更した | ❌ 整えられた |
| bridge V1（禁止文言） | **維持した** | ✅ camyu |
| bridge V2（禁止＋ショット名） | 変更した | ❌ 整えられた |
| spacewalk V1 | 変更した | ❌ 整えられた |
| spacewalk V2 | **維持した** | ✅ camyu |

**4/4 で一致：構図を変えたときだけ似顔が壊れる。**
構図の変更と似顔の崩壊は別々の不具合ではなく、**同じ挙動の裏表**。
再構図は再合成を強制し、再合成は「知らない犬」を平均に寄せる。

そして **V1 も V2 も 1勝1敗**。プロンプトでは制御できない。

### 1.3 着せ役を3つ試して、法則が確定した

`scripts/test-b3.ts` / `scripts/test-b4-kling.ts`。
**同一の素材画像**（LoRA が描いた衣装なしの camyu）に対し、着せ役だけを変えた。

| 着せ役 | camyu を知っているか | 衣装 | 似顔 |
|---|---|---|---|
| nano-banana-pro/edit | 知らない | ✅ 着せる | ❌ 整える |
| kling-image/o3 | 知らない | ✅✅ 最も正確 | ❌ 整える |
| flux-2/lora/edit (scale 1.0) | **知っている** | ❌ **3/3で着せない** | ✅ camyu |

**「この犬を知らないモデルは、描き直すとき平均的な犬種標準しか描けない」**
これが2つの独立したモデルで再現した。nano-banana 固有のクセではない。

flux LoRA edit は唯一 camyu を保ったが、LoRA が強すぎて衣装を読まなかった。
**「衣装の正確さ」と「犬の保存」がトレードオフになっている。**

**B1 だけがこのトレードオフを回避する。着せ役を分けないので、
「描き直す」瞬間が存在しない。**

> 未検証で残した唯一の可能性: flux LoRA edit の scale を 0.6 前後に下げる案。
> B1 で確定したため打ち切った。B1 に問題が出たら、ここに戻れる。

### 1.4 顔に何も載せない（実装済み）

ヘルメット・バイザー・仮面は、顔をその層越しに描き直させる。
毛質と目の形＝個体識別情報がそこで失われる。

実装済み: `WORLD_COSTUMES.deepspace` からガラスヘルメット除去（襟のリングに置換）、
`noir` からフェドーラ除去（襟を立てるに置換）、ヘルメット前提だったカット文2件の書き換え、
`claude-script.ts` に禁止ルール追加（代替の表現指針つき）。

**効果は絵で確認済み。** ヘルメット除去前の B1 は bridge が横顔・viewport が後ろ姿だったが、
除去後は3枚とも顔が正面を向き、はっきり見えるようになった。

### 1.5 構図指示は「否定形」と「顔の向き」が必要だった

- LoRA は `"medium hero shot"` だけでは**鼻先まで寄った超クローズアップ**を返した。
  `"this is NOT an extreme close-up of the face"` を足して直った
- 顔の向きを書かないと**後ろ姿・横顔**を描く。
  `"the pet's face turned toward the camera and clearly visible"` が必要

**両方とも本番のカット指示に反映すること（§2.3）。**

### 1.6 B1 に残っている2つの欠陥

**(a) 衣装の細部が揺れる。** 同一の衣装シートを渡しても、カットごとに
襟が金属リング→柔らかい布に変わる、星条旗が増える、ハーネスが増える。
「同じ系統の服」ではあるが「同じ服」ではない。→ §3

**(b) 手足が破綻する（3本脚など）。** これは似顔とは別の問題。
拡散モデルが四足動物を難しいポーズで描くときの標準的な破綻で、
**着せ役やモデルの選択では直らない。** → §4

### 1.7 学習データの質が直接効く

camyu の8枚の内訳:

| 枚数 | 内容 |
|---|---|
| 4枚 | 顔と胸のみ |
| 2枚 | ほぼ同一カット（実質1枠分） |
| 2枚 | 全身だが**服・ハーネスを着ている** |

**服を着ていない全身が1枚もない。** LoRA は体をほとんど学習できていない。
顔は完璧なのに胴と脚が毎回モデルの想像になる一因。
（§1.6(b) の破綻を悪化させるが、これだけが原因ではない）

### 1.8 手足の破綻はプロンプトで測れなかった（が、guidance は上げてはいけない）

`scripts/test-anatomy.ts`。最も破綻しやすい spacewalk で、3案×6シード=18枚。

| 案 | 設定 | 結果 |
|---|---|---|
| A 対照 | guidance 2.5 / 28步 / regular / 解剖指示なし | 曖昧なもの1枚 |
| B | A ＋ 解剖指示 | 明白な破綻なし |
| C | B ＋ guidance 4.0 / 50步 / acceleration none | 明白な破綻なし。**だが別の個体が混ざった** |

**判定できなかった。** 18枚のどれにも、以前見つけた明白な3本脚は再現しなかった。
**発生率が低すぎて n=6 では案の差が出ない。** テスト設計が甘かった。
「プロンプトで直るか」の答えは出ていない。

**ただし C は明確に不採用。** 描き込みは緻密になったが、オーナーが
「別の子が混ざっている」と指摘。`guidance_scale` はテキストへの追従度なので、
**上げるほど LoRA の個体情報よりプロンプトの一般記述が勝つ。**
似顔と引き換えに緻密さを買う交換であり、この商品では成立しない。

> 副次的な観察: 以前の3本脚は「顔をカメラに向けろ」を入れる前の
> 古いプロンプトで生成したものだった。現在の条件では再現していない。
> 因果は証明できていない。

---

## 2. 変更内容

### 2.1 ペットごとの LoRA 学習を追加する

**新しい最上流ステージ。** 注文の写真から LoRA を1つ学習し、
以降のすべての静止画生成で使う。

- モデル: `fal-ai/flux-2-trainer-v2`
- 入力: 顧客写真の ZIP、`default_caption` は
  `"photo of <trigger>, a small dog"` 形式
- **`default_caption` に犬種名を書かないこと。** 犬種名を書くと
  ベースモデルの犬種プロトタイプを呼び戻してしまい、
  §1.1 で潰した問題が別経路で戻る
- trigger word は注文ごとに一意（例: `mtpet<orderId短縮>`）。
  ベースモデルが事前知識を持たない稀な文字列であること
- `steps`: 1500（camyu の検証値。実測で調整可）
- 成果物の LoRA URL を注文に保存する。**再生成・リロールで再学習しない**
  （検証中も `--lora` で使い回して数十回の生成を賄った）

**コスト: 1注文あたり約 $2。**

**所要時間: 約45分（実測）。**

> **訂正。** 本書は当初「数分〜15分程度」と書いていた。**測らずに書いた私の誤り。**
> camyu の実測では、衣装シート完了 14:01:02 → 次の生成 14:45:51 で、
> **その間はLoRA学習のみ。約45分**（`steps: 1500`、fal のキュー待ち込み、n=1）。
>
> **この誤りは実装に波及した。** これを信じて stills タスクの `maxDuration` が
> 3600秒（60分）に設定されたが、**学習45分＋従来の絵コンテ生成（元の上限が
> 1800秒＝最大30分）で最大75分**。超える。時間切れになると
> **45分かけた学習ごと捨ててリトライで再学習**する。本書が最も避けたい
> 「注文を落とさない」に真正面から反する。→ §2.7 で解決する。
>
> `LORA_TRAIN_STEPS` を下げれば時間は減るが、**採用しない。**
> 本書 §1 の検証はすべて 1500 での結果であり、下げると
> 今日積み上げた判断の前提が崩れる。時間はタスク分割で吸収する。

> スキーマ追加が必要（LoRA URL と trigger word の保存先）。
> 本書で唯一のスキーマ変更。

### 2.2 テイク生成を B1 に置き換える

`generateTakeOnce` 相当を `fal-ai/flux-2/lora/edit` に変更。

```
model:      fal-ai/flux-2/lora/edit
loras:      [{ path: <注文のLoRA>, scale: 1.0 }]
image_urls: [<衣装シート>]
image_size: { width: 2048, height: 1152 }
```

- **`image_size` にプリセット名を使わないこと。** `landscape_16_9` は
  1024×576 になる。検証中に一度これで測ってしまった。
  明示的な width/height なら 2048 まで出る
- プロンプトで**役割を書き分ける**:
  「参照画像は衣装シート。**衣装だけ**をそこから取れ。
  **この動物の同一性はモデル側から来るのであって、参照画像から来るのではない**」
- **`IDENTITY_RULES` は入れない。** あれは「知らない犬を描くモデルを
  ドリフトから引き止める」ための文言。学習済みモデルには不要で、
  既に同意しているモデルと議論することになる

### 2.3 カット指示に「寄りすぎの否定」を足す（§1.5）

**まず現状の確認。** カメラ指示は `SHOT_FRAMINGS`（`lib/film-script.ts`）に
6カット分あり、`stills-pipeline.ts` から**プラン分岐なしで**使われている。
つまりプリセットもカスタムもここを共有している（§2.6）。

**顔の向きは既に入っていた。** 6つのうち5つが
「face ... turned toward the camera」を明示している。
§1.5 で「顔の向きを書かないと後ろ姿になる」と書いたのは、
**検証スクリプトが本番の `SHOT_FRAMINGS` ではなく、私が簡略化して書いた
framing 文を使っていた**ため。本番にこの欠陥は無い。ここは変更不要。

**実際に足すべきものは2つだけ:**

1. **寄りすぎの否定。** `SHOT_FRAMINGS[0]`（medium hero shot）は
   検証で**鼻先まで寄った超クローズアップ**を返した。
   `"this is NOT an extreme close-up of the face"` 相当を明示する。
   `SHOT_FRAMINGS[3]` は意図的なクローズアップなので**足さない**
2. **`SHOT_FRAMINGS[4]`（medium establishing）だけ「toward the camera」が無い。**
   他の5つに合わせる

**シーン記述側にカメラ指示を書かせないこと**（現行方針を維持）。
カスタムの `cuts[].scene` は動作と設定のみ。

### 2.4 解像度の扱い

B1 の出力は 2048×1152。従来の nano-banana は 2752×1536 だった。
**下がる。** 動画素材としては 1080p を上回るので実用上は足りるが、
ポスター用途で不足するなら**ポスターだけ従来経路を残す**か、
アップスケーラを挟む。実装者は現行のポスター生成が
テイク画像を流用しているかを確認し、流用しているなら影響を報告すること。

---

### 2.6 プリセットと Director's Cut は同じ構造になる（追加作業なし）

**確認済みの事実。** `lib/stills-pipeline.ts` は `resolveWorld(order)` を
呼ぶだけで、プランによる分岐を持たない。`resolveWorld` が返すのは
衣装・アーク・ロググラインだけで、**生成の仕組みそのものは両プラン共通**。
`SHOT_FRAMINGS` も同様に共有されている。

したがって **§2.1〜§2.5 と §4 は、実装した時点で両プランに同時に効く。**
Director's Cut 用に別実装するものは無い。**分岐を新設しないこと。**

顔を覆う衣装の禁止（§1.4）も両プランで実装済み:
- プリセット → `WORLD_COSTUMES` の3つを直接修正
- カスタム → `claude-script.ts` のスキーマ説明とシステムプロンプトに禁止ルール

**要オーナー判断（コスト）:** LoRA 学習は注文あたり約 $2。
これが **Preset（$99）にも乗る。** プリセットだけ旧方式に残す選択肢はあるが、
**その場合プリセットの似顔は「似ていない」ままになる**（§1.1 の CURRENT が
まさにプリセットの経路）。本書の推奨は**両プランに適用**。

---

### 2.7 LoRA 学習を別タスクに分割する（オーナー決定）

**§2.1 の45分を、1つのタスクに絵コンテ生成と同居させない。**

```
task 1: train-pet-lora   学習だけ。完了したら loraUrl/loraTriggerWord を
                         Order に保存し、task 2 を起動する
task 2: stills           既存の絵コンテ生成。Order から loraUrl を読むだけで、
                         学習は絶対にしない
```

**分割の本質は時間ではなく、失敗の隔離。** 学習結果が Order に永続化された
時点で、以降 task 2 が何度リトライしても**再学習は起きない**。
同居させたままだと、絵コンテ側の一時的な失敗が45分の学習を道連れにする。

要件:

- **task 1 が失敗しても task 2 を必ず起動すること。** `loraUrl` が null のまま
  従来の nano-banana 経路にフォールバックして注文は完走する（§8 証明5と同じ姿勢）。
  **学習の失敗を注文の失敗にしない**
- `maxDuration` は各タスクの実態に合わせる。task 2 は分割後 1800秒に戻せる
- 顧客への通知（`sendChooseStillEmail`）は **task 2 の完了時のまま**。
  学習完了では何も送らない — 顧客にとって中間状態は意味を持たない
- 既存の「`order.loraUrl` があれば再利用」チェックは**そのまま残す**。
  task 1 自体が再実行された場合の二重学習を防ぐ

---

## 3. 衣装の一貫性（§1.6(a)）

**まずプロンプトだけで対処する。** コストがかからないため。

- 衣装記述に**揺れている要素を名指しで固定**する
  （「襟は金属のリング」「国旗・追加のベルト・ハーネスは無し」）
- 衣装シートを渡す際に「**この参照からは衣装だけを取り、
  背景・ポーズ・ライティングは無視しろ**」を明示（検証で使った文言）

これで足りなければ、**衣装一致もゲートする**（§4 と同じ仕組みに相乗り）。
先にプロンプトを試すこと。

---

## 4. 手足の破綻（§1.6(b)）

### 4.1 解剖指示をプロンプトに入れる

`scripts/test-anatomy.ts` で3案×6シードを実測した（詳細は §1.8）。
**採用するのは B（既定設定のまま、文言だけ追加）。**

負のプロンプトが無いエンドポイントなので、指示は**肯定形**で書く:

> Anatomically correct dog: exactly four legs — two front legs and two hind
> legs — each attached to the body in the right place, one paw per leg. No
> extra limbs, no missing limbs, no duplicated or floating paws, no tangled or
> merged legs. The body under the suit is a single coherent dog.

**効果は証明できていない**（§1.8 の通り、n=6 では案の差が出なかった）。
コストがゼロなので、効かなくても損はしないという理由で入れる。

**`guidance_scale` を上げないこと。** §1.8 の C 案で 4.0 に上げたところ、
描き込みは緻密になったが**別の個体が混ざった**。guidance はテキスト記述への
追従度なので、上げるほど LoRA が持つ個体情報よりプロンプトの一般記述が勝つ。
**この商品では最悪の交換。既定の 2.5 のままにすること。**

`num_inference_steps` と `acceleration` は C にまとめて入れてしまったため、
guidance と切り分けて評価できていない。**単独なら有益な可能性は残っている**が、
未検証・生成時間増のため本書では採用しない。

### 4.2 解剖ゲート（採用）

**似顔ゲートと同じ場所に、同じ形で足す。**

- 判定: VLM に「脚は何本見えるか」「手足の付き方・本数に破綻はないか」を問う
- 落ちたら**顧客に見せる前に**引き直す（既存のリロール機構に相乗り）

**§4.1 が効いても、これは必要。** 理由は発生率ではなく枚数:

> 絵コンテは 6カット×3テイク＝**18枚**。
> 1枚あたりの破綻率が 10% なら、18枚すべて無事な注文は 0.9¹⁸ ≈ **15%**。
> 5% まで下がっても、まだ **60%** の注文に最低1枚混ざる。

「めったに起きない」と「顧客が一度も見ない」の間には18倍の開きがある。

**選択肢の保護でもある。** 3本脚が1枚混ざると、顧客はそれを弾くために
3つの選択肢のうち1つを消費する。実質2択に減る。$249 の商品で見せてはいけない。

**緩和策（別途、オーナー判断）:** 難しいポーズ（しがみつく・宙返り・無重力で漂う）は
破綻率が高い。プリセットのカット文を「立つ・歩く・見上げる」寄りに調整すると
発生率は下がるが、絵の面白さとのトレードオフ。**本書では変更しない。**

---

## 4.5 似顔スコアのゲートを引き下げる（採用）

**このスコアには判別力が無い。** 今日のベイクオフ15枚を、オーナーの目視判定で
2群に分けて集計した結果:

| 群 | スコア | 平均 |
|---|---|---|
| 似ていない（CURRENT・A/EDIT） | 70, 80, 70, 70, 90, 90 | **78.3** |
| 似ている（B1・B2a・B2b） | 80, 70, 80, 90, 80, 80, 80, 75, 75 | **78.9** |

**分布が重なっており、最高点の 90 が両群に出ている。**
人間が一目で区別できるものを、このスコアは区別していない。

**現行の閾値 80 は実害を出している。** 「似ている」群の約半数が 80 未満で、
つまり**正しい絵を捨てて引き直している。** リロールの時間と費用が
判別力のないノイズに払われている。

**加えて、LoRA 導入でこのゲートの存在理由が薄れる。**
ドリフトは「生成物を参照して生成する」チェーンから来ていた（`IDENTITY-FIDELITY-SPEC.md` §1.2）。
LoRA は個体を重みに焼き込むので、その経路自体が消える。

### 変更

- **閾値 80 → 50 前後に下げる。** ゲートの役割を「品質の採点」から
  **「破局の検出」**に変える。まったく別の動物・種が出た、といった
  明白な事故だけを止める
- **リロールの判断材料として使わない。** 78 と 83 の差は §4.5 の通り無意味
- `scoreClip` / 端フレーム判定（`lib/film-pipeline.ts`）も同じスコアを使っている。
  **同じ扱いにすること**

> **完全に外さない理由:** 破局の検出能力は「無い」と証明されたわけではない。
> 測ったのは「似ている/似ていない の判別力が無い」ことだけ。
> 呼び出し1回は安いので、事故対策として低い閾値で残す。
>
> **将来これを正しく測るなら:** 明白に別の犬の画像を混ぜて、
> スコアが落ちるかを見ればよい。今日のデータには「別の犬」が
> 一枚も無かった（全部 camyu を狙ったもの）ので、その検証はできていない。

---

## 5. 写真アップロード要件（§1.7）

**現状: `MIN_PHOTOS = 4` / `MAX_PHOTOS = 8`**（`components/PhotoUploadForm.tsx`）。
案内文は「正面の顔が1枚、横顔もあると良い」。

**LoRA 学習では要件が変わる。**

- トレーナーの推奨は **10枚以上**
- **服・ハーネスを着ていない全身写真**が必要。今の案内は顔の写真を推奨しており、
  顔だけ集まる。camyu の実例がまさにそれになった
- **ほぼ同一のカットは枠の無駄**。案内に「違う角度・違う日のものを」を入れる

### 決定（オーナー承認済み）

- **`MIN_PHOTOS` を 4 → 7 に上げる**
- **`MAX_PHOTOS` は 8 → 12 に上げる**（枠が増えるほど学習は安定する。
  最小を上げた以上、上限が 8 のままでは余地が狭い）
- **全身は「推奨」であって必須にしない。** 必須にすると、
  条件を満たせない顧客がアップロードを完了できず、注文自体が落ちる。
  **案内文で強く促す**にとどめる

### 案内文の書き換え

現行「正面の顔が1枚、横顔もあると良い」は**顔だけを集めてしまう**。
camyu の実例が正確にそうなった（8枚中4枚が顔と胸のみ、
服を着ていない全身はゼロ）。書き換えの要点:

1. **全身を強く促す** —「立っている全身が2枚以上あると、体の描写が安定します」
2. **服・ハーネスを着ていない写真を促す** — 服を着た写真しかないと、
   モデルはその服ごとその子だと学習する
3. **同じような写真を避けるよう促す** —「違う角度・違う日のものを」。
   ほぼ同一のカットは枠を1つ潰すだけ
4. **同じ時期の写真で揃えるよう促す**（オーナー指摘）—
   子犬期と成犬期が混ざると、LoRA は**その中間の、実在しない犬**を学習する。
   毛の長さ・体格・顔つきが平均化されるため、どの時期にも似ていない絵になる。
   camyu の検証でも、トリミング直後で毛の状態だけ違う1枚を学習から外している
5. **正面7割・その他の角度3割を目安に示す**（オーナー指摘）—
   正面だけだと横顔や斜めのカットで破綻し、角度がバラバラだと正面が甘くなる。
   絵コンテの `SHOT_FRAMINGS` は6つとも顔がカメラを向く構図なので、
   **正面を主軸にしつつ、立体を掴ませるための角度を混ぜる**のが実際の要件に合う

> 4 と 5 は「守らないとエラー」ではなく**品質の期待値を上げるための助言**。
> 検証不能な条件を必須にしても、顧客が自己申告で通すだけになる。

> トレーナー（`flux-2-trainer-v2`）の推奨は 10枚以上だが、
> 7枚を下限としたのはコンバージョンとのトレードオフによる**オーナー判断**。
> 実運用の似顔品質を見て見直す余地がある。

---

## 6. 絵コンテの納品タイミング

LoRA 学習が入るぶん、Gate 1 到達までの時間が延びる。
**オーナー既決の方針: 絵コンテが出来次第、顧客にメールで通知する。**
顧客をブラウザで待たせない。

実装者は既存のメール送信経路（Resend）と StatusPoller の扱いを確認し、
Gate 1 到達時の通知が既にあるかを報告すること。無ければ追加する。

---

## 7. 触るファイル

| ファイル | 変更 |
|---|---|
| `lib/stills-pipeline.ts` | LoRA 学習ステージ追加、`generateTakeOnce` を B1 に、framing 文言（§2.3）、解剖ゲート（§4） |
| `lib/film-script.ts` | 衣装記述の固定化（§3）※ヘルメット・帽子除去は実装済み |
| `lib/claude-script.ts` | 衣装記述の固定化指示（§3）※顔禁止ルールは実装済み |
| `prisma/schema.prisma` | LoRA URL / trigger word の保存 |
| `components/PhotoUploadForm.tsx` | 枚数と案内文（§5、**要承認**） |
| `lib/identity.ts` | 解剖判定の追加（似顔判定と同じ形で） |

**変更しないもの**: 状態機械、Gate 1/2 のフロー、動画の編集エンジン
（EDL・エンコード・SFX）、B2 の安全網、Stripe / Printify 周り。

---

## 8. 検証

**DB に接続しない。** 実測は本番注文でのみ可能。実装者はコード読解による証明と
静的チェックまでを担当する。

- `tsc` / `eslint` / `next build`
- `scripts/test-assemble.ts` は無関係だが**壊さないこと**
- 証明すべきこと:
  1. LoRA が注文ごとに1回だけ学習され、リロールで再学習されない
  2. `image_size` が明示的な width/height であり、プリセット名でない
  3. テイク生成のプロンプトに `IDENTITY_RULES` が入っていない
  4. 解剖ゲートが**顧客に見せる前**に効いている
  5. LoRA 学習が失敗した場合に**従来経路へフォールバック**し、注文を落とさない

**合格基準は実測とオーナーの目視。** 本書のすべての判断がそうやって決まった。
スコアは §1.1 の通り方式選択には使えない。

---

## 9. オーナー決定事項（実装者はこれに従うこと）

| 論点 | 決定 |
|---|---|
| Preset にも LoRA を適用するか | **適用する。** 両プランとも似顔に注力（§2.6） |
| 写真の枚数 | **最小7・最大12。全身は推奨、必須にしない**（§5） |
| 難しいポーズを減らすか | **減らさない。現状維持**（§4.2 の緩和策は実施しない） |
| 似顔スコアのゲート | **閾値を下げ、破局検出に役割変更**（§4.5） |

---

## 10. 未解決のまま残すもの

- **flux LoRA edit の scale を下げる案**（§1.3）— B1 に問題が出たときの退路
- **`num_inference_steps` / `acceleration` の単独評価**（§4.1）—
  guidance と抱き合わせで測ってしまったため切り分けできていない
- **手足の破綻の実発生率**（§1.8）— n=6 では測れなかった。
  30枚規模で測れば §4.1 の要否がはっきりするが、
  結果に関わらず §4.2 のゲートは必要
- **似顔スコアの破局検出能力**（§4.5）— 明白に別の犬を入れたら
  落ちるかは未検証
- **ポスターの解像度**（§2.4）— 現行実装の確認待ち
