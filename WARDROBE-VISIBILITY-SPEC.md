# 衣装（ワードローブ）可視化 仕様書 — Director's Cut

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-07-29
> 対象は **Director's Cut（custom）のみ**。Preset は対象外（§5）。

---

## 1. 問題

Director's Cut では**衣装をClaudeが勝手に決めている**。オーナーの実テスト
（camyu / 1980年代シティポップ東京）で、パステルのオーバーサイズブレザー＋
ストライプT＋丸メガネ＋ゴールドネックレスという衣装が生成されたが、
**オーナーはそれを衣装だと認識せずに承認してしまった**。

なぜ気づけなかったか:

- ブリーフの入力項目は4つ（舞台／雰囲気／見せ場／結末）で、**衣装の欄がない**
- 衣装は `treatmentText` の**散文の中に埋もれている**（1文の括弧内）
- 「これは今から決まる衣装で、変更できる」と示すラベルもボタンもない

さらに構造上の問題として、**トリートメント承認（Gate 0）が衣装の確定点**に
なっている。承認後は Gate 1（テイク選択）に進み、そこには衣装を戻す導線が
一切ない。つまり **気づかないまま承認したら二度と変えられない**。

これは実装バグではなく**プロダクト設計の欠落**。承認画面は「撮影前に方向性を
固める」ためのゲート（`PRICING-PRODUCT-V2-SPEC.md` §3.5 A）なのに、
確定する要素のうち衣装だけが可視化されていない。

---

## 2. 対応方針（案B：承認画面での可視化）

**衣装を独立したラベル付き項目として承認画面に表示する。**

決定的に都合がよいのは、**衣装が既に構造化データとして保存されている**こと:
`Order.generatedScript.costume`（`WorldBundle.costume` — `lib/film-script.ts`）。
散文から抽出する必要はなく、**Claudeのプロンプト変更もスキーマ変更も不要**。

### なぜ案A（ブリーフに入力欄を追加）を先にやらないか

案A＝「衣装・見た目の希望（任意）」を intake の5つ目に足す案は有効だが後回し:

- 案Bだけで「気づかないまま承認」は防げる（＝今回の実害を消せる）
- 「監督が提案 → 施主が見て直す」は実際の制作フローに近く、空欄を埋めさせる
  より体験がよい。強い要望がある人は修正依頼で言えば同じ結果になる
- 実装量・リスクが小さく、既存の修正ループ（無制限の文章直し）に乗るだけ

案Aは案Bの後に、往復を1回減らす最適化として検討する（§6）。

---

## 3. 実装内容（案B）

### 3.1 承認画面に「WARDROBE」セクションを追加

`components/TreatmentApproval.tsx` に、トリートメント本文の**上**（先に目に入る
位置）へ衣装ブロックを置く。

- ラベル: `WARDROBE — worn in every shot`（全カット共通であることを明示。
  「1着で固定」は品質の根拠なので、制約ではなく**売り**として書く）
- 本文: `generatedScript.costume` の文字列そのまま
- 「Not what you pictured? Ask for a different look below.」等、変更可能である
  ことを1行で示す（修正依頼への導線）
- 衣装が取れない場合（旧注文・生成失敗）はセクションを**出さない**（空欄を
  見せない）

見た目は既存のシネマ調（`border-hairline` / `bg-surface` / `font-display` の
小見出し）に合わせる。金枠で強調しすぎず、本文より前に読ませるだけでよい。

### 3.2 修正依頼の入力欄にヒントを追加

`showRevise` のテキストエリアの `placeholder` を、衣装も直せると分かる例文に:

> 例: `Make the jacket a leather one` / `Change cut 3 to a rainy street`

（現行のプレースホルダがカット変更のみを示唆しているなら差し替える。）

### 3.3 データの受け渡し

`app/approve/[token]/page.tsx` の `AWAITING_TREATMENT_APPROVAL` 分岐で、
`order.generatedScript` から `costume` を取り出して prop で渡す。

```tsx
// generatedScript は Json。custom 以外や旧注文では null なので、
// 取り出しは防御的に（型が合わなければ undefined 扱い）。
const bundle = order.generatedScript as unknown as WorldBundle | null;
<TreatmentApproval
  orderId={order.id}
  approveToken={order.approveToken}
  petName={petName}
  treatmentText={order.treatmentText ?? ""}
  costume={typeof bundle?.costume === "string" ? bundle.costume : null}
/>
```

`TreatmentApproval` の props に `costume: string | null` を追加する（クライアント
コンポーネントなので Prisma 型は渡さず、プリミティブのみ — 既存方針どおり）。

### 3.4 admin にも同じものを

`app/admin/[orderId]/page.tsx` の「Director's Cut — brief & treatment」セクションに
`衣装（全カット共通）` の行を追加。サポート対応時に「顧客が承認した衣装」を
即確認できるようにする（読み取り専用、操作なし）。

---

## 4. Claude 側に必ず残す制約（既存プロンプトの再確認）

案Bでは Claude のプロンプトを変更しないが、**衣装が顧客に見え、修正依頼が
飛んでくる**ようになるため、既存ガード（`lib/claude-script.ts` rule 1/3）が
効き続けることを実装時に確認する:

1. **1着で全カット統一** — 衣装は `costume` 1フィールドで、`cuts[].scene` には
   衣装を書かない。「カットごとに違う服」という要望が来ても**1着に正規化**する
   （複数衣装は同一人物性が崩れ、シリーズもののトレーラーに見えなくなる）
2. **顔を隠す衣装は不可** — マスク・フード・大きなサングラス・全頭ヘルメット等。
   本人判定ゲート（Gemini VLM）が落ちて**無償再制作＝コスト爆弾**になる。
   要望が来たら「顔が見える等価物」に言い換える（例: 全頭ヘルメット →
   透明バイザーのヘルメット。Preset の deepspace が透明ヘルメットで成立して
   いるのが実例）
3. **IP/フランチャイズ模倣は不可**（既存 rule 2 の範囲）— 特定作品の制服・
   ロゴ・キャラ衣装は、オリジナルの等価物に言い換える

必要なら rule 1 に「顔を覆う衣装を避ける」旨を**衣装の文脈でも**明記する
（現状は主にシーン構図の文脈で書かれている）。プロンプト変更を伴うので、
実装者は変更前後で1回ずつ生成して劣化がないか確認する。

---

## 5. Preset は対象外（意図的）

Preset Worlds は世界ごとに衣装固定（`WORLD_COSTUMES`）で、それが商品定義。
衣装まで選べることを **Director's Cut 専用の価値**として残す。

LPコピーに使える差分になる（例: 「your story, your world, **your wardrobe**」）。
`components/PricingTeaser.tsx` の Director's Cut 側の箇条書きに衣装の選択権を
加えるのは、この仕様と同時でも後でもよい。

---

## 6. 今後（この仕様の範囲外）

- **案A: ブリーフに「衣装・見た目の希望（任意）」を追加** — 往復を1回減らす。
  空欄なら従来どおり Claude が世界観に合わせて決める。`customBrief` は1本の
  文字列なので**スキーマ変更は不要**（クライアント側で連結するだけ）
- **承認後に衣装を変える導線** — Gate 0 を通過したら衣装は確定、という現在の
  仕様を維持するか、絵コンテ段階から Gate 0 に戻す道を作るかは B2（リロール
  計数・部分返金）の設計と一体で決める。安易に戻せるようにすると生成コストの
  上限が消えるので、**B2 の3回リロール枠と同じ計数に載せる**のが筋
- **顧客向けの絵コンテ再生成（リロール）** — 現状**未実装**。Gate 1 は3テイクから
  選ぶだけで、どのテイクも駄目な場合の作り直し導線がない（B2 の項目）。
  既存の `kickShotRerender` は **admin 専用・Gate 2 で完成動画の1ショットを
  差し替える**もので、絵コンテのテイク再生成ではない
