# 映像品質 v3 仕様書 — 画質・フレーミング・start+endフレーム補間

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-07-29
> 前提: `TRAILER-EDIT-SPEC.md`（ビート編集 v2）は実装・デプロイ済み。
> 本仕様はその上で残った**3つの品質課題**を潰す。
> **スキーマ変更なし**（新データは `filmArtifacts` Json 内）。

---

## 0. 背景 — v2実装後の実写レビューで判明した残課題

オーナーが新編集エンジンの出力を確認して報告した内容:

1. **顔（耳・頭頂部）が画面外に切れているカットがある** → §1
2. **画質が少し悪い** → §2
3. **犬が映らないインサートが1枚も入らなかった** → §3
4. **ずっと舌を出していてクールに見えない** → §4
5. **動きが乏しく「安っぽい」** → §5（本命）

1〜4は設定・プロンプト修正のみで**生成コスト増ゼロ**。5は終了フレーム6枚分
（1本あたり十数円）だけ増える。

---

## 1. 顔が切れる — クロップの二重掛けを解消

### 1.1 原因（確定）

`lib/film-pipeline.ts` で**中央クロップが2段**かかっている:

- `gradeFilterChain(matte=true)`: `crop=iw:iw/2.35` → **高さの約24%**を上下から削除
- `renderClipBeat` のパンチイン: `crop=iw/1.35:ih/1.35` → さらに**約13%**を上下から削除

合計で上端から約25%が消える。ところが `SHOT_FRAMINGS` は「顔を大きく・フレーム
上部寄りに」置く設計なので、**耳と頭頂部が構造的に切られる**。

### 1.2 対応（確定）

**(a) 2.35:1 シネスコマットを廃止する。**
`MATTE_ASPECT` とマット適用（`crop`＋`pad`）を削除し、16:9 全画面を維持する。
理由: **顔が商品そのもの**なのに画面の24%を捨てるのは本末転倒。映画感はカード・
カット割り・SFXで十分成立している（v2で確認済み）。グレイン・グレード・
ビネットは**残す**。

> マットを将来また試したい場合に備え、削除ではなくフラグで残す実装も可。ただし
> **既定は必ずオフ**。オンにするなら §1.2(b) の上寄せクロップと併用が前提。

**(b) パンチインを「上寄せクロップ」かつ控えめにする。**
- `PUNCH_IN_ZOOM` を **1.35 → 1.2**
- クロップ位置を中央ではなく**上寄り**に: `crop=iw/Z:ih/Z:(iw-ow)/2:(ih-oh)*PUNCH_IN_Y_BIAS`
  で `PUNCH_IN_Y_BIAS = 0.25`（＝削る分の3/4を下から削る）。被写体の頭部を守り、
  重要度の低い下端（足元・地面）を優先的に捨てる
- 定数にコメントで理由を明記（「中央クロップは頭を切る。SHOT_FRAMINGS が顔を
  上部に置くため」）

---

## 2. 画質 — エンコード設定と世代劣化

### 2.1 原因（確定）

| # | 問題 | 詳細 |
|---|---|---|
| A | **`-crf` 未指定** | すべての `libx264` 呼び出しが `-preset veryfast` のみ＝x264既定のCRF 23相当。納品物として不足 |
| B | **グレイン×低ビットレート** | `noise=alls=6` はビットレートを大量に食う。CRF 23ではグレインが潰れ**ブロックノイズ化**する（＝「フィルム質感」が「圧縮の汚れ」になる） |
| C | **再エンコード4段** | 正規化 → ビート切り出し → 連結 → グレード。各段CRF 23で**世代劣化が累積** |
| D | **アップスケール二重** | Kling standard は720p出力。`scale=1920:1080` で拡大 → さらにパンチイン1.35倍で**実質533p相当**を1080pに引き伸ばし |
| E | **`fps=24` への間引き** | `fps` フィルタは単純間引き。素材が30fpsならカクつきだけが増える |

### 2.2 対応（確定）

**(a) CRFを明示する。** 定数化して1箇所で管理:
- **最終出力**（グレード段＋mux前の最後のビデオエンコード）: `-crf 17`、`-preset slow`
- **中間ファイル**（正規化・ビート切り出し・カード・Ken Burns・連結）: `-crf 14`、
  `-preset fast`
  → 中間を高品質に保つことで世代劣化を止める。CRF 14 は視覚的にほぼ可逆

**(b) 再エンコード段数を減らす。**
最低限、**ビート切り出しとグレードを1パスに統合**する（ビート単位でグレードを
かけるのではなく、連結後に1回グレード＝現行の設計は維持しつつ、正規化と
ビート切り出しを1パスにまとめられるなら統合する）。
統合が難しい箇所は (a) の高CRF中間で劣化を抑える方針で可。**段数削減より
CRF明示の方が効果が大きい**ので、無理な統合でロジックを壊さないこと。

**(c) グレインを弱める。** `GRAIN_STRENGTH` **6 → 3**。CRF改善後に再評価する前提で
コメントを残す。

**(d) fps変換をやめる。** `fps=24`（正規化）と `KEN_BURNS_FPS=24` を見直し、
**素材のfpsを維持**する。ただし連結には全ビートのfps統一が必要なので、
**基準fpsを素材の実fps（Klingは30fps想定）に合わせる**か、`FILM_FPS = 30` 定数に
統一する。24へ落とす処理は削除。カード・Ken Burns も同じ `FILM_FPS` を使う。

**(e) パンチインの解像度負担を軽くする。** §1.2(b) の 1.2倍化がそのまま効く
（1.35倍 → 1.2倍で拡大率が大幅に低下）。
加えて `renderClipBeat` のスケールに `flags=lanczos` を指定（拡大時の甘さ低減）。

**(f) Kling pro（1080p）を維持。** 既に `KLING_MODEL` 既定が pro。旧standard製の
クリップは720pなので、**新規生成分から改善する**旨をコメントに残す。

ファイルサイズは 60秒で 15MB → 40MB 程度に増える見込み。ダウンロード納品なので
許容（$249の納品物として適正）。

---

## 3. インサートが入らない — custom のフォールバック

### 3.1 原因（確定・バグではない）

テスト注文の `generatedScript` は `inserts` フィールド追加**前**に生成されたため、
`resolveWorld().inserts` が `[]` → EDLがインサートビートを落とした（設計通りの
graceful degradation、尺は60秒維持）。

### 3.2 ただし構造的な穴がある

`resolveWorld` は preset なら `pickWorldInserts(world, orderId)` で必ず3件返すが、
**custom は `bundle.inserts ?? []`** で、`world` が null なのでプールへの
フォールバックがない。つまり **Claudeが `inserts` を返さなかった custom 注文は
永久にB-rollゼロ**（＝一番高い$249プランが一番寂しい映像になる）。

### 3.3 対応（確定）

**custom で `bundle.inserts` が空のとき、`bundle.cuts[].scene` から情景インサートを
導出する。**

- 実装: `resolveWorld` 内（または専用ヘルパー）で、`cuts` から **3つのシーン文を
  決定的に選び**（`stableHash(order.id)` で開始位置を決めて等間隔に3つ）、それぞれを
  「その場所の情景（動物・人物なし）」に変換する短い接頭辞つき文字列にする。
  例: `` `the setting of: ${scene} — empty scene, no animals, no people` ``
- `generateInsertStill()` 側は既に「no animals / no people」を prompt に折り込む
  実装なので、追加の依存は不要（Claude呼び出しもnano-banana以外の新APIも不要）
- preset の挙動は**変更しない**（`WORLD_INSERTS` プールのまま）
- どちらのプランでも「3件用意できなければインサート無しで60秒に正規化」の
  フォールバックは維持する

---

## 4. 舌を出している — 静止画プロンプトの制御

### 4.1 原因

`lib/stills-pipeline.ts` の `generateTakeOnce` プロンプトに**口・舌に関する指示が
一切ない**ため、モデルが「犬らしさ」として舌を出した絵を作る。今回の
「1980年代シティポップの都会的でクールな世界観」には合わない。

### 4.2 対応（確定）

**性格（personality）で分岐する。**
- `playful` → 舌を出した表情は**正しい**（変更しない）
- それ以外（`brave` / `easygoing` / `timid`、および **custom（personality が null）**）
  → プロンプトに「**mouth closed, no lolling tongue, composed expression**」を追加

実装場所は `generateTakeOnce` の呼び出し側（性格を知っている層）から文字列を
渡す形が素直。定数化してコメントに理由（世界観との不一致）を書く。

**注意**: これは**新規に生成する静止画にのみ効く**。既存注文には影響しない
（＝今のテスト注文で確認するには reshoot が必要）。

---

## 5. 【本命】start+end フレーム補間 — 「本当に動く」映像へ

### 5.1 なぜこれが構造的な突破口か

現在のジレンマ:
- 動かさない → 死んだ映像（v2実装後もここが残った）
- 動かす → 本人が崩れる（ヨー回転で実証済み。スコア60）

**start+end 補間はこのジレンマを壊す。** このパイプラインの**実証済みの強み**
（静止画は本人に似せられる・本人判定ゲートを通せる）を、**実証済みの弱み**
（動画モデルが未知の姿を勝手に創作する）の解決に転用できるため:

- **開始フレーム**: 顧客承認済みの絵コンテ（本人保証済み）
- **終了フレーム**: **静止画として先に生成**し、**本人判定ゲートを通す**
- 動画モデルの仕事は「AとBを埋める」だけ → 自由創作の余地が激減

これで初めて「**横断歩道を渡る**」「**グラスを掲げて乾杯する**」のような、
ブリーフにあった**本当のアクション**が本人性を保ったまま出せる。§4の舌問題も
終了フレーム側で口を閉じさせれば整う。

### 5.2 終了フレームの生成

- モデル・手法は既存の `generateTakeOnce`（`EDIT_MODEL` = nano-banana編集）を**流用**。
  参照画像に **(1) その開始フレーム自身 (2) ヒーローシート** を渡すことで、衣装・
  背景・個体を保ったまま**ポーズだけ変えた1枚**を作る
- プロンプトは「**同じ場面の数秒後**」であることを明示し、**変化を1つに絞る**:
  ```
  The FIRST reference image is this exact frame of the film. Generate the SAME
  scene a few seconds later: identical pet, identical costume, identical
  location, lighting and camera framing. The ONLY change: <endPose>.
  ```
- `<endPose>` は**カット毎の控えめな終点**を定義した定数配列（`SHOT_END_POSES`、
  `lib/film-script.ts`）。`SHOT_MOTIONS` と対になる設計:
  - **ヨー回転は禁止**（`getShotMotion` と同じ理由。顔の向きを変えない）
  - 例: 「一歩前に出た」「座っていたのが立ち上がった」「顎を上げた」
    「前足を上げた」「口を閉じて正面を見据えた」
  - **始点と終点が離れすぎると中間でモーフィングが起きる**ので、差は小さく縛る
    （これを定数のコメントに明記）
- **本人判定ゲートを通す**: 既存 `scoreIdentity`/`scoreFrame` で終了フレームを採点し、
  閾値未満なら**別シードで1回リロール**、それでも駄目なら**そのカットは
  start+end を諦めて従来の単一フレーム i2v にフォールバック**（絶対に落とさない）
- キャッシュ: `filmArtifacts.endFrameUrls?: string[]`（再開・再アセンブル・
  単一ショット再生成で再利用。Json内なのでマイグレーション不要）

### 5.3 動画モデルへの投入

- Kling の image-to-video に**終了フレームを渡す入力（tail image / end frame）が
  あるかは実装時に fal のスキーマで確認**すること。**オフラインでは確定できない**
- **ある場合**: `start_image_url` ＋ 終了フレーム入力を両方渡す
- **ない場合**: `KLING_MODEL` と同じ要領で **start+end に対応した別モデルを
  この用途にだけ使う**（env で切替可能に。例 `KLING_MODEL_INTERPOLATE`）。
  これが健全な「モデル混成」— 投機ではなく**具体的な選定基準（A→Bをきれいに
  埋められるか）**に基づく選択になる
- **どちらも不可なら §5 は実装せず、その事実を報告する**（無理に代替実装しない）

### 5.4 段階導入（重要）

**全6カットを一度に切り替えない。**
- `SHOT_END_POSES` に `null` を許し、**null のカットは従来どおり単一フレーム i2v**
- 初期値は**動きが物語上重要な2カットのみ**を start+end にする（例: cut 2 と cut 6）
- 効果を確認してから拡大する。定数を1行変えるだけで増減できる設計にする

### 5.5 コスト

追加は**終了フレームの静止画生成のみ**（採用カット数×1枚、1枚あたり数円）。
Klingの本数・秒数は不変。§2 のCRF変更も生成コストには影響しない。

---

## 6. 触るファイル

| ファイル | 変更 |
|---|---|
| `lib/film-pipeline.ts` | §1.2 マット廃止・パンチイン上寄せ1.2倍／§2 CRF定数・fps統一・lanczos・グレイン3／§5.2 終了フレーム生成＋ゲート＋キャッシュ／§5.3 動画モデル入力 |
| `lib/film-script.ts` | §3.3 custom のインサート導出／§5.2 `SHOT_END_POSES` 追加 |
| `lib/stills-pipeline.ts` | §4.2 口閉じ指示（性格分岐） |
| `.env.example` | §5.3 で別モデルを使う場合のみ env 追記 |
| `scripts/test-assemble.ts` | §2(d) fps統一・§1 マット廃止に追随（**尺60秒アサーションは維持**） |

**変更しないもの**: スキーマ／状態機械／Gate 1・Gate 2／ポスターパイプライン／
`getShotMotion`／EDLのビート構成（§1.2(b)のズーム値以外）／SFX設計。

---

## 7. 検証

**静的**: `tsc` / `eslint` / `next build`。

**ローカル実測**（DB・fal・Trigger.dev不使用）: `scripts/test-assemble.ts` を維持・更新し、
1. マスター／9:16 とも **尺がちょうど60.0秒**（±1フレーム）
2. インサート有り／無しの両方で60.0秒
3. SFX欠如時のフォールバック
4. **追加**: パンチインビートのクロップが**上寄せ**になっていること（EDL/フィルタ
   文字列のアサーションで可）、**マットのcropが消えている**こと
5. **追加**: 出力ファイルサイズが以前より増えている（CRF が効いている証跡）

**start+end（§5）は fal を叩かないと検証できない**ので、ローカルでは
「終了フレームが無い/生成失敗のときに従来経路へフォールバックする」ことを
型と分岐で確認し、実挙動はオーナーの本番テストに委ねる旨を報告に明記すること。

---

## 8. 実装順序の推奨

1. **§1（顔切れ）＋§2（画質）** — 設定のみ・即効・既存クリップで効果確認可
2. **§3（インサート）＋§4（舌）** — 小さく独立
3. **§5（start+end）** — 最も効果が大きいが未知が残る。§5.3 の確認を最初に行い、
   不可なら早期に報告する

§1〜§4 と §5 は**別コミットに分けること**（§5が不可でも§1〜§4を確実に残すため）。
