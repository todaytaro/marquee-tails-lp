# 似顔精度 仕様書 — 「うちの子だ！」が成立していない問題

> **ステータス: 設計のみ / 未実装。** 実装は別途（Sonnet 等）。作成: 2026-08-01
> **スキーマ変更なし。** 既存の `uploadedPhotoUrls` を使い切るだけ。
> 追加生成コストはほぼゼロ（判定APIの呼び出しが1回あたり画像1枚増えるのみ）。

---

## 0. 問題（オーナー実写比較）

オーナーが自分の犬（camyu / ミニチュアシュナウザー）の実写真と、Gate 1 の
生成テイクを並べて **「似てなくね？」**。

実物の特徴: 頭頂部がボサボサに伸びた柔らかい毛、丸い顔、大きな目、全体に
ふわふわで幼い印象。

生成物: **整えられた一般的なミニチュアシュナウザー**。トリミング済みのような
毛並み、シャープな顔立ち、標準的な体型。

つまり `IDENTITY_RULES` が明示的に禁じている
**「generic breed look へのドリフト」がそのまま起きている。**

**これは商品価値の核心が成立していないという意味。** コード自身のコメントが
「Likeness is THE conversion moment（"that's my pet!"）」と書いている、その
conversion moment が機能していない。他のあらゆる品質改善（編集・音・尺・
インサート）より優先度が高い。

---

## 1. 原因（推測ではなく、コードで確認した事実）

### 1.1 判定が「実物」と比較していない

`lib/identity.ts` の `scoreIdentity(portraitUrl, candidateUrl)` のプロンプトは
こう書かれている:

> **"Image 1 is the REAL pet."**

**しかし渡されているのは `portraitUrl` — AI生成のポートレート**である
（`lib/stills-pipeline.ts` の `generateGatedTake` → `scoreIdentity(portraitUrl, url)`）。
コードが自分自身に嘘をついている。

### 1.2 ドリフトが3世代積み重なる

```
顧客の実写真（最大6枚使用）
   ↓ 生成① generateIdentityPortrait   image_urls = photoUrls        （実写真ベース）
   ↓ 生成② generateHeroSheet          image_urls = [portrait, …]    （生成物ベース）
   ↓ 生成③ generateTakeOnce           image_urls = [hero, portrait, photo0]
判定    scoreIdentity(portrait, take)  ← 実写真と一度も比較しない
```

- 生成②③の参照は**主に生成物**。実写真は生成③で **`photo0` の1枚だけ**、
  しかも**3番目**に入る（`refs = [heroRef, portraitRef, photo0]`）。
  `generateTakeOnce` のプロンプトは「**FIRST** reference image is the
  definitive look」と明言しているので、**実写真の優先度は最下位**。
- そして判定は生成①（ポートレート）との一致度。
  **①が実物から離れていれば、②③がそれに忠実であるほど高得点が出る。**

これが「スコア80〜85なのに似ていない」の構造的な理由。ゲートは
**「ドリフトした基準との整合性」**を測っており、**似顔を測っていない**。

### 1.3 特徴記述が個体差ではなく犬種を拾いやすい

`analyzePhotos` の description は「coat colors / fur texture / face shape …」を
1文70語で求める。**犬種名で説明できてしまう要素**に寄りやすく、
「頭頂部の毛が伸びてボサボサ」「顔が丸い」「毛が柔らかく幼い」といった
**この個体を他のシュナウザーと区別する情報**が落ちる。

---

## 2. 対応方針

### 2.1 【最重要】判定を実写真に対して行う（§1.1の是正）

`scoreIdentity` の第1引数に **顧客の実写真** を渡す。プロンプトの
「Image 1 is the REAL pet」が**初めて真実になる**。

- 実装: `generateGatedTake` に実写真URL（`bestFrontalIndex` の1枚）を渡し、
  `scoreIdentity(realPhotoUrl, takeUrl)` で採点する
- ポートレートとの一致度も**併せて記録して構わない**（一貫性の指標として
  有用）が、**ゲートの合否は実写真スコアで決める**
- **閾値は据え置き（80）で始める。** 基準が厳しくなるのでスコアは下がる
  はずで、リロールが増える。**それが正しい挙動**（今まで通していたものが
  通らなくなる）。ただし全カットが3回リロールしても通らない状態は避けたい
  ので、**実測してから閾値を調整する**方針を明記する（下げるのではなく、
  §2.2〜2.4で生成の質を上げて通す）

### 2.2 実写真を参照の先頭に持ち上げる（§1.2の是正）

`generateTakeOnce` の `refs` 順序を変え、**実写真を1番目**にする。

現状: `[heroRef, portraitRef, photo0]` — プロンプトが「FIRST が決定版」と
言っているので、いまは**ヒーローシートが決定版**になっている。

変更後の考え方:
- **実写真を1枚目**（＝この個体の決定版の見た目）
- **ヒーローシートは衣装の参照**として2枚目以降に置き、プロンプト側で
  「1枚目は個体の見た目の決定版、2枚目は衣装の決定版」と**役割を明示的に
  書き分ける**（今は「FIRST が costume も fur も全部の決定版」と書いてある）
- 実写真は**複数枚渡す**ことを検討（現状 `photo0` の1枚のみ）。ただし
  参照画像を増やすと衣装の一貫性が落ちるリスクがあるので、
  **実写真2枚 + ヒーローシート1枚**あたりから実測する

### 2.3 個体差を言語化する（§1.3の是正）

`analyzePhotos` のプロンプトに、**犬種で説明できない要素**を明示的に求める
項目を追加する。例:

- 「この個体を**同じ犬種の他の個体と見分ける**ために最も重要な3点」
- 「毛の**伸び具合・整い具合**（トリミング直後か、伸びているか）」
- 「顔の**丸さ／シャープさ**、目の**大きさ**の印象」

`mouth` / `tail` / `ears` を verbatim で固定しているのと同じ発想を、
**「犬種プロトタイプから外れている点」**に適用する。

### 2.4 ポートレート生成自体の忠実度（ドリフト①の是正）

`generateIdentityPortrait` は実写真を直接参照しているので構造的には正しいが、
**ここでドリフトすると全部が狂う**ため、忠実度を最優先にする:

- プロンプトに「**トリミングして整えるな／実際の毛の伸び具合をそのまま
  再現せよ**」を明示（現状 `IDENTITY_RULES` の "do NOT groom them
  differently" は既にあるが、生成結果を見る限り効いていない — より具体的に
  書く）
- **ポートレート自体にも実写真との判定ゲートをかける**（現状は素通し）。
  ここが通らなければ何度作り直しても無駄なので、**パイプラインの最上流に
  ゲートを置くのが最も費用対効果が高い**

---

## 3. 実装順序（費用対効果の順）

1. **§2.1 判定を実写真基準に** — 数行。これだけで「気づかずに出荷」が止まる
2. **§2.4 ポートレートにゲート** — 上流の品質が下流全部を決めるため
3. **§2.2 参照順序と役割の書き分け** — プロンプト変更のみ
4. **§2.3 個体差の言語化** — プロンプト変更のみ

すべて**プロンプトと参照の渡し方の変更**で、スキーマ変更なし・追加生成
コストはほぼゼロ（判定の画像が1枚増えるだけ）。

---

## 4. 触るファイル

| ファイル | 変更 |
|---|---|
| `lib/stills-pipeline.ts` | `generateGatedTake` の採点基準（§2.1）、`refs` 順序（§2.2）、`analyzePhotos` プロンプト（§2.3）、ポートレートのゲート（§2.4） |
| `lib/identity.ts` | `scoreIdentity` の引数名を実態に合わせる（`portraitUrl` → `referenceUrl` 等）。プロンプトは「Image 1 is the REAL pet」のまま**正しくなる** |
| `lib/film-pipeline.ts` | 動画クリップの採点も同じ問題を抱えているか確認し、抱えていれば同様に是正（`scoreFrame` の第1引数） |

**変更しないもの**: スキーマ、状態機械、Gate 1/2 のフロー、動画の編集
エンジン（EDL・エンコード・SFX）、B2 の安全網、ポスターパイプライン。

---

## 5. 検証

**DBに接続しない。** fal を叩く実測は本番注文でのみ可能なので、実装者は
**コード読解による証明**と静的チェックまでを担当する:

- `tsc` / `eslint` / `next build`
- **証明すべきこと**（コード読解で示す）:
  1. `generateGatedTake` の合否判定が**実写真**を参照している
  2. `generateTakeOnce` の1枚目が**実写真**である
  3. ポートレートが実写真とのゲートを通っている
  4. 実写真が存在しない/取得失敗のとき、**従来動作にフォールバック**して
     注文を落とさない（アップロード必須なので通常あり得ないが、防御的に）
- `scripts/test-assemble.ts` は無関係だが**壊さない**こと

**実測はオーナーの本番テスト**（Preset 1本 or Director's Cut 1本）で行い、
**同じ犬の実写真と並べて判断**する。スコアの数字ではなく、
**オーナーが「似ている」と言うか**が唯一の合格基準。

---

## 6. 認識しておくべきこと

§2 を全部やっても **モデルの上限は残る**。nano-banana が「この個体の毛の
伸び具合」をどこまで再現できるかには限界があり、**似顔が完璧になる保証は
ない**。

ただし現状は**構造的な欠陥で似顔を測ることすらできていない**状態なので、
まず測れるようにする。測れるようになって初めて「モデルの限界」と
「実装の不備」を切り分けられる。
